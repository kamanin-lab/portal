# Hilfe FAQ Page Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the placeholder `/hilfe` page with a FAQ page grouped into 6 category sections, each using an animated accordion for questions and answers.

**Architecture:** Static data in `hilfe-faq-data.ts` feeds two new components (`FaqItem`, `FaqSection`) that follow existing portal card+accordion patterns. `HilfePage.tsx` renders the sections with Motion stagger-on-scroll animations. No backend, no search, no contact block.

**Tech Stack:** React 19, TypeScript, Motion (`motion/react`), Hugeicons (`@hugeicons/react` + `@hugeicons/core-free-icons`), Tailwind CSS v4, Vitest + React Testing Library

**Design spec:** `docs/superpowers/specs/2026-03-31-hilfe-faq-design.md`

---

## File Map

| Action | Path | Responsibility |
|--------|------|---------------|
| Create | `src/shared/lib/hilfe-faq-data.ts` | Types + all FAQ content as typed array |
| Create | `src/shared/components/help/FaqItem.tsx` | Single accordion item (question + animated answer) |
| Create | `src/shared/components/help/FaqSection.tsx` | Category card (icon + heading + list of FaqItem) |
| Modify | `src/shared/pages/HilfePage.tsx` | Full page — replaces placeholder |
| Create | `src/shared/components/help/__tests__/FaqItem.test.tsx` | FaqItem unit tests |
| Create | `src/shared/components/help/__tests__/FaqSection.test.tsx` | FaqSection unit tests |
| Create | `src/shared/pages/__tests__/HilfePage.test.tsx` | HilfePage integration test |

---

## Task 1: FAQ data types and content

**Files:**
- Create: `src/shared/lib/hilfe-faq-data.ts`

- [ ] **Step 1: Create the data file with types and all 6 sections**

```ts
// src/shared/lib/hilfe-faq-data.ts
import type { ComponentType } from 'react'

export interface FaqItemData {
  question: string
  answer: string
}

export interface FaqSectionData {
  id: string
  title: string
  iconName: string   // Hugeicons export name — resolved at component level
  items: FaqItemData[]
}

export const FAQ_SECTIONS: FaqSectionData[] = [
  {
    id: 'projekte',
    title: 'Projekte',
    iconName: 'FolderOpen01Icon',
    items: [
      {
        question: 'Was bedeuten die Projektphasen?',
        answer:
          'Jedes Projekt durchläuft vier Phasen: Konzept, Struktur, Design und Entwicklung. Diese helfen Ihnen, den Fortschritt auf einen Blick zu verfolgen. Jede Phase enthält Aufgaben, die schrittweise abgeschlossen werden.',
      },
      {
        question: 'Wie verfolge ich den Fortschritt meines Projekts?',
        answer:
          'Im Projektbereich sehen Sie alle Phasen und deren Aufgaben mit aktuellem Status. Abgeschlossene Schritte werden farblich markiert.',
      },
      {
        question: 'Was bedeutet „Ihre Rückmeldung"?',
        answer:
          'Dieser Status zeigt an, dass eine Aufgabe Ihre Freigabe oder Ihr Feedback benötigt, bevor wir weitermachen können. Bitte reagieren Sie zeitnah, damit sich Ihr Projekt nicht verzögert.',
      },
      {
        question: 'Wie erteile ich eine Freigabe oder fordere Änderungen an?',
        answer:
          'Öffnen Sie die entsprechende Aufgabe und klicken Sie auf „Freigeben". Wenn Sie Änderungen wünschen, wählen Sie „Änderungen anfordern" und beschreiben Sie Ihre Anmerkungen.',
      },
    ],
  },
  {
    id: 'tickets',
    title: 'Tickets & Anfragen',
    iconName: 'CustomerService01Icon',
    items: [
      {
        question: 'Wie erstelle ich ein neues Support-Ticket?',
        answer:
          'Klicken Sie auf „Neue Anfrage" im Bereich Tickets. Füllen Sie den Titel, die Beschreibung und die Priorität aus und bestätigen Sie mit „Senden".',
      },
      {
        question: 'Welche Prioritätsstufen gibt es?',
        answer:
          'Es gibt vier Stufen: Niedrig, Normal, Hoch und Dringend. Dringend ist für kritische Probleme, die sofortige Aufmerksamkeit erfordern. Bitte nutzen Sie diese Stufe nur wenn wirklich nötig.',
      },
      {
        question: 'Wie verfolge ich den Status meiner Anfragen?',
        answer:
          'Im Bereich Tickets sehen Sie alle Ihre Anfragen mit aktuellem Status (Offen, In Bearbeitung, Ihre Rückmeldung, Abgeschlossen). Sie erhalten eine Benachrichtigung bei jeder Statusänderung.',
      },
      {
        question: 'Kann ich einem Ticket eine Nachricht anhängen?',
        answer:
          'Ja. Öffnen Sie das Ticket und verwenden Sie das Kommentarfeld, um Nachrichten oder zusätzliche Informationen hinzuzufügen.',
      },
    ],
  },
  {
    id: 'dateien',
    title: 'Dateien',
    iconName: 'FolderCloudIcon',
    items: [
      {
        question: 'Wo finde ich meine Projektdateien?',
        answer:
          'Im Projektbereich unter dem Tab „Dateien". Alle Dokumente, Designs und Lieferobjekte werden dort organisiert.',
      },
      {
        question: 'Wie lade ich eine Datei hoch?',
        answer:
          'Navigieren Sie zum Dateien-Tab und klicken Sie auf „Datei hochladen". Sie können einzelne Dateien oder mehrere gleichzeitig hochladen.',
      },
      {
        question: 'Kann ich Ordner erstellen?',
        answer:
          'Ja. Klicken Sie auf „Neuer Ordner", geben Sie einen Namen ein und bestätigen Sie. Ordner helfen Ihnen, Dateien thematisch zu strukturieren.',
      },
      {
        question: 'Welche Dateiformate werden unterstützt?',
        answer:
          'Das Portal unterstützt gängige Formate wie PDF, DOCX, XLSX, PNG, JPG, ZIP und viele weitere. Bei speziellen Formaten wenden Sie sich bitte an uns.',
      },
    ],
  },
  {
    id: 'kredite',
    title: 'Kredite',
    iconName: 'CreditCardIcon',
    items: [
      {
        question: 'Was sind Kredite und wie funktionieren sie?',
        answer:
          'Kredite sind Ihr Guthaben für Leistungen von KAMANIN. Jede Anfrage oder Aufgabe verbraucht je nach Aufwand eine bestimmte Anzahl an Krediten.',
      },
      {
        question: 'Wo sehe ich mein aktuelles Guthaben?',
        answer:
          'Ihr aktuelles Kreditguthaben wird in der Seitenleiste angezeigt. Detaillierte Informationen finden Sie unter Konto → Kredite.',
      },
      {
        question: 'Was passiert, wenn mein Guthaben aufgebraucht ist?',
        answer:
          'Bei niedrigem Guthaben erhalten Sie eine Benachrichtigung. Wenden Sie sich an uns, um Ihr Paket aufzustocken. Laufende Projekte werden nicht unterbrochen.',
      },
    ],
  },
  {
    id: 'benachrichtigungen',
    title: 'Benachrichtigungen',
    iconName: 'Notification01Icon',
    items: [
      {
        question: 'Wann erhalte ich eine Benachrichtigung?',
        answer:
          'Sie erhalten Benachrichtigungen bei Statusänderungen Ihrer Tickets, neuen Kommentaren, Freigabeanfragen und wichtigen Projektaktualisierungen.',
      },
      {
        question: 'Wie markiere ich Benachrichtigungen als gelesen?',
        answer:
          'Öffnen Sie den Benachrichtigungsbereich (Glocke-Symbol) und klicken Sie auf eine Benachrichtigung, um sie zu lesen. Sie können auch alle auf einmal als gelesen markieren.',
      },
    ],
  },
  {
    id: 'konto',
    title: 'Konto & Einstellungen',
    iconName: 'UserCircleIcon',
    items: [
      {
        question: 'Wie ändere ich mein Passwort?',
        answer:
          'Gehen Sie zu Konto (unten links in der Seitenleiste) und wählen Sie „Passwort ändern". Sie erhalten eine E-Mail mit einem Link zum Zurücksetzen.',
      },
      {
        question: 'Wie aktualisiere ich meine E-Mail-Adresse?',
        answer:
          'Unter Konto → Profil können Sie Ihre E-Mail-Adresse ändern. Sie erhalten eine Bestätigungsmail an die neue Adresse.',
      },
      {
        question: 'Wie melde ich mich ab?',
        answer:
          'Klicken Sie auf Ihr Profilbild unten in der Seitenleiste und wählen Sie „Abmelden".',
      },
    ],
  },
]
```

- [ ] **Step 2: Commit**

```bash
git add src/shared/lib/hilfe-faq-data.ts
git commit -m "feat(hilfe): add FAQ data types and content"
```

---

## Task 2: FaqItem component

**Files:**
- Create: `src/shared/components/help/FaqItem.tsx`
- Create: `src/shared/components/help/__tests__/FaqItem.test.tsx`

- [ ] **Step 1: Write the failing tests**

```tsx
// src/shared/components/help/__tests__/FaqItem.test.tsx
import { render, screen, fireEvent } from '@testing-library/react'
import { FaqItem } from '../FaqItem'

describe('FaqItem', () => {
  const props = {
    question: 'Was bedeuten die Projektphasen?',
    answer: 'Jedes Projekt durchläuft vier Phasen.',
  }

  it('renders the question text', () => {
    render(<FaqItem {...props} />)
    expect(screen.getByText(props.question)).toBeInTheDocument()
  })

  it('does not show answer by default', () => {
    render(<FaqItem {...props} />)
    expect(screen.queryByText(props.answer)).not.toBeInTheDocument()
  })

  it('shows answer after clicking the question button', () => {
    render(<FaqItem {...props} />)
    fireEvent.click(screen.getByRole('button'))
    expect(screen.getByText(props.answer)).toBeInTheDocument()
  })

  it('hides answer again when clicking a second time', () => {
    render(<FaqItem {...props} />)
    fireEvent.click(screen.getByRole('button'))
    expect(screen.getByText(props.answer)).toBeInTheDocument()
    fireEvent.click(screen.getByRole('button'))
    expect(screen.queryByText(props.answer)).not.toBeInTheDocument()
  })

  it('renders a separator by default', () => {
    const { container } = render(<FaqItem {...props} />)
    expect(container.firstChild).toHaveClass('border-b')
  })

  it('does not render a separator when isLast=true', () => {
    const { container } = render(<FaqItem {...props} isLast />)
    expect(container.firstChild).not.toHaveClass('border-b')
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run test -- src/shared/components/help/__tests__/FaqItem.test.tsx
```

Expected: FAIL — `FaqItem` does not exist yet.

- [ ] **Step 3: Create the FaqItem component**

```tsx
// src/shared/components/help/FaqItem.tsx
import { useState } from 'react'
import { motion, AnimatePresence } from 'motion/react'
import { HugeiconsIcon } from '@hugeicons/react'
import { ArrowDown01Icon } from '@hugeicons/core-free-icons'
import { cn } from '@/shared/lib/utils'

interface FaqItemProps {
  question: string
  answer: string
  isLast?: boolean
}

export function FaqItem({ question, answer, isLast = false }: FaqItemProps) {
  const [isOpen, setIsOpen] = useState(false)

  return (
    <div className={cn(!isLast && 'border-b border-[var(--border-light)]')}>
      <button
        onClick={() => setIsOpen((prev) => !prev)}
        className="w-full flex items-center justify-between py-3 text-left hover:bg-[var(--surface-hover)] rounded-[var(--r-sm)] px-2 -mx-2 transition-colors"
      >
        <span className="text-sm font-medium text-[var(--text-primary)] leading-snug pr-4">
          {question}
        </span>
        <HugeiconsIcon
          icon={ArrowDown01Icon}
          size={16}
          className={cn(
            'text-[var(--text-tertiary)] shrink-0 transition-transform duration-200',
            isOpen && 'rotate-180'
          )}
        />
      </button>

      <AnimatePresence>
        {isOpen && (
          <motion.div
            initial={{ height: 0, opacity: 0 }}
            animate={{ height: 'auto', opacity: 1 }}
            exit={{ height: 0, opacity: 0 }}
            transition={{ duration: 0.2, ease: 'easeInOut' }}
            className="overflow-hidden"
          >
            <p className="text-sm text-[var(--text-secondary)] leading-relaxed pt-1 pb-3 px-0">
              {answer}
            </p>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  )
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run test -- src/shared/components/help/__tests__/FaqItem.test.tsx
```

Expected: All 6 tests PASS.

> **Note on AnimatePresence + jsdom:** Motion renders animated content immediately in jsdom (no real animation). `queryByText` after exit works because Motion removes the element synchronously in test environment. If tests are flaky, wrap the hide assertion in `await waitFor(() => ...)` from `@testing-library/react`.

- [ ] **Step 5: Commit**

```bash
git add src/shared/components/help/FaqItem.tsx src/shared/components/help/__tests__/FaqItem.test.tsx
git commit -m "feat(hilfe): add FaqItem accordion component"
```

---

## Task 3: FaqSection component

**Files:**
- Create: `src/shared/components/help/FaqSection.tsx`
- Create: `src/shared/components/help/__tests__/FaqSection.test.tsx`

> **Icon note:** The `iconName` field in `FaqSectionData` is a string (Hugeicons export name). `FaqSection` receives the resolved icon directly as a prop — the caller (HilfePage) resolves `iconName → icon` by importing from `@hugeicons/core-free-icons`. This keeps `FaqSection` decoupled from the data file.

- [ ] **Step 1: Write the failing tests**

```tsx
// src/shared/components/help/__tests__/FaqSection.test.tsx
import { render, screen } from '@testing-library/react'
import { FaqSection } from '../FaqSection'
import { HelpCircleIcon } from '@hugeicons/core-free-icons'

const section = {
  id: 'test',
  title: 'Projekte',
  icon: HelpCircleIcon,
  items: [
    { question: 'Frage 1?', answer: 'Antwort 1.' },
    { question: 'Frage 2?', answer: 'Antwort 2.' },
  ],
}

describe('FaqSection', () => {
  it('renders the section title', () => {
    render(<FaqSection section={section} />)
    expect(screen.getByText('Projekte')).toBeInTheDocument()
  })

  it('renders all questions', () => {
    render(<FaqSection section={section} />)
    expect(screen.getByText('Frage 1?')).toBeInTheDocument()
    expect(screen.getByText('Frage 2?')).toBeInTheDocument()
  })

  it('renders correct number of FaqItems', () => {
    render(<FaqSection section={section} />)
    expect(screen.getAllByRole('button')).toHaveLength(2)
  })
})
```

- [ ] **Step 2: Run tests to verify they fail**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run test -- src/shared/components/help/__tests__/FaqSection.test.tsx
```

Expected: FAIL — `FaqSection` does not exist yet.

- [ ] **Step 3: Create the FaqSection component**

Note: `FaqSection` accepts `icon` (resolved Hugeicons icon component), not `iconName`. The type for the prop is defined locally here.

```tsx
// src/shared/components/help/FaqSection.tsx
import { HugeiconsIcon } from '@hugeicons/react'
import type { FaqItemData } from '@/shared/lib/hilfe-faq-data'
import { FaqItem } from './FaqItem'

interface FaqSectionProps {
  section: {
    id: string
    title: string
    icon: React.ComponentType<React.SVGProps<SVGSVGElement>>
    items: FaqItemData[]
  }
}

export function FaqSection({ section }: FaqSectionProps) {
  return (
    <section className="bg-[var(--surface)] rounded-[14px] border border-[var(--border)] p-5">
      <div className="flex items-center gap-2 mb-3">
        <HugeiconsIcon
          icon={section.icon}
          size={18}
          className="text-[var(--text-secondary)]"
        />
        <h2 className="text-sm font-semibold text-[var(--text-primary)]">
          {section.title}
        </h2>
      </div>

      <div className="border-b border-[var(--border)] mb-1" />

      <div className="flex flex-col">
        {section.items.map((item, index) => (
          <FaqItem
            key={item.question}
            question={item.question}
            answer={item.answer}
            isLast={index === section.items.length - 1}
          />
        ))}
      </div>
    </section>
  )
}
```

- [ ] **Step 4: Run tests to verify they pass**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run test -- src/shared/components/help/__tests__/FaqSection.test.tsx
```

Expected: All 3 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add src/shared/components/help/FaqSection.tsx src/shared/components/help/__tests__/FaqSection.test.tsx
git commit -m "feat(hilfe): add FaqSection card component"
```

---

## Task 4: Update HilfePage

**Files:**
- Modify: `src/shared/pages/HilfePage.tsx`
- Create: `src/shared/pages/__tests__/HilfePage.test.tsx`

**Icon resolution:** HilfePage imports the 6 Hugeicons by name and builds a resolved section list. If an icon name does not exist in `@hugeicons/core-free-icons`, substitute `HelpCircleIcon` as fallback and leave a `// TODO: replace with correct icon` comment.

The 6 icons to verify: `FolderOpen01Icon`, `CustomerService01Icon`, `FolderCloudIcon`, `CreditCardIcon`, `Notification01Icon`, `UserCircleIcon`. Check they exist by attempting the import; if TypeScript reports an error, use `HelpCircleIcon` as a placeholder.

- [ ] **Step 1: Write the failing test**

```tsx
// src/shared/pages/__tests__/HilfePage.test.tsx
import { render, screen } from '@testing-library/react'
import { MemoryRouter } from 'react-router-dom'
import { HilfePage } from '../HilfePage'

function renderPage() {
  return render(
    <MemoryRouter>
      <HilfePage />
    </MemoryRouter>
  )
}

describe('HilfePage', () => {
  it('renders the page heading', () => {
    renderPage()
    expect(screen.getByRole('heading', { name: 'Hilfe & FAQ' })).toBeInTheDocument()
  })

  it('renders all 6 section titles', () => {
    renderPage()
    expect(screen.getByText('Projekte')).toBeInTheDocument()
    expect(screen.getByText('Tickets & Anfragen')).toBeInTheDocument()
    expect(screen.getByText('Dateien')).toBeInTheDocument()
    expect(screen.getByText('Kredite')).toBeInTheDocument()
    expect(screen.getByText('Benachrichtigungen')).toBeInTheDocument()
    expect(screen.getByText('Konto & Einstellungen')).toBeInTheDocument()
  })

  it('renders at least 18 question buttons across all sections', () => {
    renderPage()
    expect(screen.getAllByRole('button').length).toBeGreaterThanOrEqual(18)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run test -- src/shared/pages/__tests__/HilfePage.test.tsx
```

Expected: FAIL — page renders placeholder content.

- [ ] **Step 3: Rewrite HilfePage**

First check which icons exist — run a quick TypeScript check or just attempt imports. If `CustomerService01Icon` or others are missing, use `HelpCircleIcon` as fallback (the spec says to verify during implementation).

```tsx
// src/shared/pages/HilfePage.tsx
import { motion } from 'motion/react'
import {
  FolderOpen01Icon,
  // CustomerService01Icon, — check if exists, fallback to below
  HelpCircleIcon,
  // FolderCloudIcon,      — check if exists
  FolderLibraryIcon,
  // CreditCardIcon,       — check if exists
  Notification01Icon,
  UserCircleIcon,
} from '@hugeicons/core-free-icons'
import { ContentContainer } from '@/shared/components/layout/ContentContainer'
import { FaqSection } from '@/shared/components/help/FaqSection'
import { FAQ_SECTIONS } from '@/shared/lib/hilfe-faq-data'

// Map iconName strings from FAQ_SECTIONS data to actual Hugeicons components.
// Check each import above — if it fails to compile, substitute HelpCircleIcon.
const ICON_MAP: Record<string, React.ComponentType<React.SVGProps<SVGSVGElement>>> = {
  FolderOpen01Icon,
  CustomerService01Icon: HelpCircleIcon, // replace if icon exists
  FolderCloudIcon: FolderLibraryIcon,    // replace if icon exists
  CreditCardIcon: HelpCircleIcon,        // replace if icon exists
  Notification01Icon,
  UserCircleIcon,
}

export function HilfePage() {
  const resolvedSections = FAQ_SECTIONS.map((section) => ({
    ...section,
    icon: ICON_MAP[section.iconName] ?? HelpCircleIcon,
  }))

  return (
    <ContentContainer width="narrow">
      <div className="p-6 max-[768px]:p-4 flex flex-col gap-5">
        <div>
          <h1 className="text-xl font-semibold text-[var(--text-primary)]">
            Hilfe & FAQ
          </h1>
          <p className="mt-1 text-sm text-[var(--text-secondary)]">
            Antworten auf häufige Fragen zum Portal
          </p>
        </div>

        {resolvedSections.map((section, index) => (
          <motion.div
            key={section.id}
            initial={{ opacity: 0, y: 8 }}
            whileInView={{ opacity: 1, y: 0 }}
            viewport={{ once: true, margin: '-40px' }}
            transition={{ duration: 0.3, delay: index * 0.05 }}
          >
            <FaqSection section={section} />
          </motion.div>
        ))}
      </div>
    </ContentContainer>
  )
}
```

> **Icon resolution:** After writing the file, run `npm run build` briefly to check for TypeScript import errors. Fix any missing icons by replacing with `HelpCircleIcon` (or the correct name). You can search available icons by running: `grep -r "CustomerService" node_modules/@hugeicons/core-free-icons/dist/ --include="*.d.ts" | head -5`

- [ ] **Step 4: Resolve actual icon names**

Run this to find the correct names:

```bash
cd "g:/01_OPUS/Projects/PORTAL"
grep -r "CustomerService\|FolderCloud\|CreditCard" node_modules/@hugeicons/core-free-icons/dist/index.d.ts | head -20
```

Update `ICON_MAP` in `HilfePage.tsx` with any found names. For any not found, keep `HelpCircleIcon` as fallback.

- [ ] **Step 5: Run tests to verify they pass**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run test -- src/shared/pages/__tests__/HilfePage.test.tsx
```

Expected: All 3 tests PASS.

- [ ] **Step 6: Run all help-related tests**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run test -- src/shared/components/help src/shared/pages/__tests__/HilfePage.test.tsx
```

Expected: All 12 tests PASS.

- [ ] **Step 7: Run full build to verify no TypeScript errors**

```bash
cd "g:/01_OPUS/Projects/PORTAL" && npm run build
```

Expected: Build completes with no errors. Warnings are acceptable.

- [ ] **Step 8: Commit**

```bash
git add src/shared/pages/HilfePage.tsx src/shared/pages/__tests__/HilfePage.test.tsx
git commit -m "feat(hilfe): implement FAQ page with accordion sections"
```

---

## Verification Checklist

After all tasks complete:

1. `npm run dev` → navigate to `/hilfe` in browser
2. All 6 section cards visible with icons and titles
3. Click any question → answer expands smoothly, chevron rotates 180°
4. Click again → answer collapses smoothly
5. Multiple questions can be open simultaneously (independent)
6. Sections animate on scroll (fade in + slight upward slide, staggered)
7. Resize to 375px width → layout holds, text readable, touch targets comfortable
8. `npm run build` → no TypeScript errors
