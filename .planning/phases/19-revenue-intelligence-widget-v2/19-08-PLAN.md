---
phase: 19
plan: 08
type: execute
wave: 3
depends_on: [19-07]
files_modified: []
autonomous: false
requirements:
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - WIDG-QA-01
  - WIDG-QA-02

must_haves:
  truths:
    - "public/sandbox-proxy.html contains 'kmn/theme/' relay (2 grep matches — comment on line 68 + early-return guard on line 69) per PORT-02 (Pattern S7)"
    - "git diff src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx shows zero diff (empty) per PORT-04 hard constraint"
    - "Theme publisher useThemePublisher.ts exists in PORTAL and is unchanged since Phase 18 per PORT-03"
    - "McpErrorBoundary.tsx exists in PORTAL and catches AppRenderer throws; rendered error UI is German with reload button per PORT-05"
    - "Staging deployment at https://staging.portal.kamanin.at/umsatz-intelligenz renders 4 blocks in order HeuteBlock → HeatmapBlock → RepeatBlock → BasketOrAovBlock (Deliverable #1)"
    - "Staging widget renders under 2s first-paint measured via DevTools PerformanceObserver per WIDG-QA-01 (Deliverable #2)"
    - "Staging dashboard loaded at 4 clock times (09:00 / 11:00 / 14:00 / 17:00 Europe/Vienna) on a seeded day shows no universally-negative pace indicator — the -85% bug is not reproducible per WIDG-QA-02 (Deliverable #3)"
    - "With one ability sabotaged (heatmap or repeat) the widget renders 3 healthy blocks + 1 skeleton per WIDG-QA-03 (Deliverable #4)"
    - "?mock=basket-aov forces BasketOrAovBlock into aov_bands mode on the dev harness per Deliverable #5"
    - "Period-toggle end-to-end: clicking [4 Wochen] / [12 Wochen] on staging triggers mcp-proxy POST with {name:'weekly_heatmap', arguments:{weeks:N}} per D-19-05"
  artifacts:
    - path: "G:/01_OPUS/Projects/PORTAL/public/sandbox-proxy.html"
      provides: "PORT-02 verification target — relay block intact"
      contains: "kmn/theme/"
    - path: "G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx"
      provides: "PORT-04 verification target — zero diff since main"
    - path: "G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/hooks/useThemePublisher.ts"
      provides: "PORT-03 verification target — hook unchanged since Phase 18"
    - path: "G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx"
      provides: "PORT-05 verification target — German error + reload button"
  key_links:
    - from: "staging widget iframe"
      to: "PORTAL sandbox-proxy.html kmn/theme relay"
      via: "postMessage"
      pattern: "kmn/theme/"
    - from: "widget HeatmapBlock period toggle"
      to: "PORTAL mcp-proxy EF → upstream MCP server"
      via: "app.callServerTool → @mcp-ui/client onCallTool → useMcpProxy.callTool"
      pattern: "name: \"weekly_heatmap\""
---

<objective>
Final phase gate: run all PORTAL-side zero-diff / zero-TS-change verifications (PORT-02..05) and the human UAT checkpoints that require live staging + wall-clock time (WIDG-QA-01, WIDG-QA-02, period-toggle end-to-end, McpErrorBoundary throw test). No PORTAL code is modified in this plan. This is the hand-off between "widget ships" and "phase passes verification" — after plan 08 signs off, `/gsd-verify-work` can run.

Purpose: Plans 01-07 are blind to the portal runtime. Plan 08 proves that the widget actually loads inside the portal, propagates theme tokens intact, survives remount, renders 4 blocks against seeded data, and does not reproduce the -85% bug. The three manual UAT items (09:00/11:00/14:00/17:00 pace check, McpErrorBoundary forced throw, theme publisher remount observation) exist BECAUSE the automated widget tests cannot reach these behaviors — they require real-time wall clock, real mcp-proxy round trips, and real DOM mount/unmount cycles in the portal.

Output: Zero code files. A verification log + a single human-verify checkpoint at the end. PORTAL repo stays at zero diff for widget code (PORT-04 hard constraint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@G:/01_OPUS/Projects/PORTAL/.planning/phases/19-revenue-intelligence-widget-v2/19-CONTEXT.md
@G:/01_OPUS/Projects/PORTAL/.planning/phases/19-revenue-intelligence-widget-v2/19-VALIDATION.md
@G:/01_OPUS/Projects/PORTAL/.planning/phases/19-revenue-intelligence-widget-v2/19-PATTERNS.md
@G:/01_OPUS/Projects/PORTAL/.planning/phases/19-revenue-intelligence-widget-v2/19-07-SUMMARY.md
@G:/01_OPUS/Projects/PORTAL/public/sandbox-proxy.html
@G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx
@G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/hooks/useThemePublisher.ts
@G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx

<interfaces>
<!-- Interfaces the executor verifies (READ only — no modification). -->

### PORT-02 target file: PORTAL/public/sandbox-proxy.html
Expected structure (from Phase 18 verification):
- Contains a `window.addEventListener('message', ...)` handler
- Handler checks `e.data.type.indexOf('kmn/theme/') === 0` and relays the message
- Comment on line ~68 describes the relay
- Guard on line ~69 is the early-return forwarding `kmn/theme/*` messages bidirectionally

### PORT-04 target file: PORTAL/src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx
MUST have zero diff vs `main` branch after Phase 19 lands. `git diff main -- src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx` must produce zero lines of output.

### PORT-05 target file: PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx
Expected behavior (from Phase 18 / project_revenue_intelligence_module):
- React class component extending ErrorBoundary
- getDerivedStateFromError catches thrown errors
- Render fallback has German copy + a reload button (`window.location.reload()`)

### Staging deployment URL
- Widget lives on mcp-poc Vercel: `https://mcp-poc-three.vercel.app` (staging ref per project_revenue_intelligence_module memory)
- Portal page serving the widget: `https://staging.portal.kamanin.at/umsatz-intelligenz`
- Test user: MBM user + Yuri's org (per Phase 18 project_revenue_intelligence_module — only these two orgs have revenue-intelligence visible on staging)

### Phase 17 locked tool contract
- `weekly_heatmap` is in PORTAL's mcp-proxy ALLOWED_TOOLS list (landed Phase 17-05). No update needed in Phase 19.
- Period toggle calls flow: widget → AppBridge → @mcp-ui/client → useMcpProxy → mcp-proxy EF → upstream

### Project memory constraints (already honoured by widget-side plans; re-verified here)
- feedback_branching_workflow — PORTAL changes (if any arise from UAT) push directly to `staging` branch; no feature branches.
- feedback_automated_e2e_tests — NO Playwright for Phase 19; human UAT bridges the gap per D-19-10 trade-off.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated zero-diff + grep verifications for PORT-02..05</name>
  <files></files>
  <read_first>
    - G:/01_OPUS/Projects/PORTAL/public/sandbox-proxy.html (full — confirm kmn/theme relay lines)
    - G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx (full — confirm no changes planned for Phase 19)
    - G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/hooks/useThemePublisher.ts (full — confirm hook exists + useMemo stabilization)
    - G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx (full — confirm German fallback + reload button)
    - G:/01_OPUS/Projects/PORTAL/.planning/phases/19-revenue-intelligence-widget-v2/19-PATTERNS.md "Pattern S7 PORTAL zero-diff verification" — script template
  </read_first>
  <action>
    Working directory is context-specific for each command below — use absolute paths.

    Run each verification independently and record the output in a per-verification bullet list for 19-08-SUMMARY.md. If any check fails, STOP — the phase has a defect that must be routed back to a prior plan before proceeding to Task 2 (human UAT).

    **PORT-02 — sandbox-proxy kmn/theme relay intact:**
    ```bash
    grep -nc "kmn/theme/" G:/01_OPUS/Projects/PORTAL/public/sandbox-proxy.html
    ```
    Expected: count ≥ 2 (comment line + guard line). If 0, the Phase 18 relay was regressed — STOP and re-open Phase 18 scope.

    **PORT-03 — useThemePublisher.ts unchanged since Phase 18:**
    ```bash
    cd G:/01_OPUS/Projects/PORTAL && git log --oneline -10 -- src/modules/revenue-intelligence/hooks/useThemePublisher.ts
    ```
    Expected: no commits after the Phase 18 merge commit (approximate: post-2026-04-24 there should be no edits attributed to Phase 19 plans). If commits appear with `feat(19-*)` prefix, investigate — PORT-03 requires the publisher to survive multiple mounts unchanged.

    Also grep for the useMemo stability pattern per feedback_react_hook_identity_churn memory:
    ```bash
    grep -c "useMemo" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/hooks/useThemePublisher.ts
    ```
    Expected: at least 1 (hook returns a stable object).

    **PORT-04 — RevenueIntelligencePage.tsx ZERO DIFF vs main:**
    ```bash
    cd G:/01_OPUS/Projects/PORTAL && git diff main -- src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx
    ```
    Expected: empty output (zero bytes). This is the hard-stop constraint of Phase 19. If any diff exists, Phase 19 has failed its zero-TS-diff assertion — STOP.

    Alternative if `main` branch is not directly comparable (e.g. staging is ahead of main), compare against the Phase 18 final commit:
    ```bash
    cd G:/01_OPUS/Projects/PORTAL && git log --oneline --all -- src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx | head -5
    ```
    The most recent commit touching this file should be Phase 18's wire-up commit (per project_revenue_intelligence_module memory). If a Phase 19 commit touches this file, it's a PORT-04 violation.

    **PORT-05 — McpErrorBoundary renders German + reload button (static check):**
    ```bash
    # German error copy present
    grep -cE "(Fehler|konnte nicht|neu laden|erneut)" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx
    # Reload mechanism present
    grep -c "window.location.reload" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx
    # No English leaks
    grep -cE "(Something went wrong|Reload|Retry)" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx
    ```
    Expected:
    - German grep count ≥ 2 (at least 2 German phrases present)
    - Reload grep count ≥ 1
    - English grep count === 0

    If any expectation fails, STOP and flag — the error boundary is a PORT-05 hard requirement.

    Record all 4 verification results in the 19-08-SUMMARY.md draft (per-check pass/fail + measured values).

    No git commit — this task is read-only verification.
  </action>
  <verify>
    <automated>cd G:/01_OPUS/Projects/PORTAL && git diff main -- src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx | wc -c | awk '$1==0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nc "kmn/theme/" G:/01_OPUS/Projects/PORTAL/public/sandbox-proxy.html` returns ≥ 2 (PORT-02)
    - `grep -c "useMemo" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/hooks/useThemePublisher.ts` returns ≥ 1 (PORT-03 — identity stability)
    - `cd G:/01_OPUS/Projects/PORTAL && git diff main -- src/modules/revenue-intelligence/components/RevenueIntelligencePage.tsx` produces zero output (PORT-04)
    - `grep -cE "(Fehler|konnte nicht|neu laden|erneut)" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx` returns ≥ 2 (PORT-05 German copy)
    - `grep -c "window.location.reload" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx` returns ≥ 1 (PORT-05 reload mechanism)
    - `grep -cE "(Something went wrong|Reload|Retry)" G:/01_OPUS/Projects/PORTAL/src/modules/revenue-intelligence/components/McpErrorBoundary.tsx` returns `0` (PORT-05 no English leaks)
  </acceptance_criteria>
  <done>All 4 PORT-* automated verifications green with numeric evidence captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT on staging — 5 behaviors that require live runtime</name>
  <files>none (human verification on live staging — no code changes in this task)</files>
  <action>Human verification checkpoint — see <how-to-verify> below for the 5 required UAT items (UAT-1 through UAT-5) and 2 optional items (UAT-6, UAT-7). Perform each UAT against the live staging deployment, record results, and return the structured resume-signal message. If any required UAT fails, stop and flag the failing plan for revision.</action>
  <verify><automated>Manual verification — see resume-signal format in task. No automated command runs for human-verify checkpoint.</automated></verify>
  <done>All 5 required UATs (1-5) return pass in resume-signal, OR documented deferral with routing for any environment-dependent failure.</done>
  <what-built>
    Widget v2 is deployed:
    - mcp-poc repo pushed to main (auto-deploys to `mcp-poc-three.vercel.app` per project_revenue_intelligence_module).
    - PORTAL `staging` branch already wired to embed that widget at `/umsatz-intelligenz`.
    - 4 blocks (HeuteBlock → HeatmapBlock → RepeatBlock → BasketOrAovBlock) compose the dashboard.
    - Period toggle on HeatmapBlock exercises the full MCP round-trip.
    - BlockSkeleton error variant kicks in per-block when an upstream ability fails.
    - Theme tokens flow from portal → sandbox-proxy → widget via `kmn/theme/*`.
    - McpErrorBoundary catches thrown errors and shows a German reload screen.
  </what-built>
  <how-to-verify>
    All tests performed as the MBM user (or Yuri's org admin) on `https://staging.portal.kamanin.at`.

    **UAT-1 — First-paint latency (WIDG-QA-01, Deliverable #2):**
    1. Open Chrome DevTools → Performance tab on `https://staging.portal.kamanin.at/umsatz-intelligenz`.
    2. Start recording. Hard-reload the page (Ctrl+Shift+R).
    3. Look for the `daily_briefing` tool-call start and the final `onSizeChanged` firing in the timeline.
    4. PASS CONDITION: elapsed time from tool-call start to final `onSizeChanged` ≤ 2000 ms.
    5. Repeat 3× — report median.

    **UAT-2 — -85% bug non-reproducible (WIDG-QA-02, Deliverable #3):**
    1. Ensure Summerfield DDEV is reachable and seeded (Phase 15 prerequisite). If DDEV is not reachable, skip this UAT item and flag as external dependency — cannot be verified against seeded Summerfield until DDEV mount is restored; the bug-fix logic is however verified by the D-19-06 confidence branch tests in plan 04.
    2. On staging at 09:00 Europe/Vienna (local time), hard-reload `/umsatz-intelligenz`, screenshot the `HeuteBlock` pace indicator.
    3. Repeat at 11:00, 14:00, 17:00 on the SAME seeded day (synthetic seeder clock).
    4. PASS CONDITION: none of the 4 renders shows a universally-negative pace indicator (the v1 -85% bug signature). Confidence branch may render `Schätzung` caption at 09:00 if baseline is sparse — that is correct behaviour per D-19-06, not a failure.

    **UAT-3 — Per-block error isolation (WIDG-QA-03, Deliverable #4):**
    1. Manually sabotage one ability on the upstream — e.g. temporarily edit the `kmn-revenue-abilities` plugin to make `kmn/repeat-metrics` return HTTP 500; OR set a kill-switch WP option.
    2. Reload the portal page.
    3. PASS CONDITION: RepeatBlock renders the error skeleton (`Daten nicht verfügbar` + `Bitte Seite neu laden`); the other 3 blocks (HeuteBlock, HeatmapBlock, BasketOrAovBlock) render normally.
    4. Restore the ability after verification. Reload to confirm the 4-block healthy state returns.

    **UAT-4 — BasketOrAovBlock aov_bands mode (Deliverable #5):**
    1. Open the dev harness: `https://mcp-poc-three.vercel.app/widgets/daily-briefing/dev-host.html?mock=basket-aov`.
    2. PASS CONDITION: BasketOrAovBlock renders the AOV bands layout (3 horizontal bars with twin opacities, `Ø Bestellwert`, `Median Bestellwert`) — not the basket pair layout.
    3. Open `?mock=` (no param) — verify it reverts to market_basket_product mode (Boxspringbett + Lattenrost pairs).

    **UAT-5 — Period-toggle end-to-end wiring (D-19-05):**
    1. Open DevTools → Network tab filtered by `mcp-proxy`.
    2. On staging `/umsatz-intelligenz`, click `[4 Wochen]` in HeatmapBlock.
    3. Observe the Network panel: one POST to the mcp-proxy EF.
    4. Inspect the request body — JSON payload must include `{"method":"tools/call", "params":{"name":"weekly_heatmap","arguments":{"weeks":4}}}` (or equivalent JSON-RPC shape).
    5. PASS CONDITION: the heatmap dims to 60% opacity during fetch, re-renders with new data after ≤ 2s; sibling blocks (Heute / Repeat / Basket) do NOT re-render (check by watching React DevTools render highlights — siblings stay still).
    6. Repeat with `[12 Wochen]` — same flow, different payload.

    **UAT-6 (optional, diagnostic) — Theme publisher survives remount (PORT-03):**
    1. Open DevTools → Console.
    2. Paste one-liner to log postMessages: `window.addEventListener('message', (e) => { if (typeof e.data?.type === 'string' && e.data.type.indexOf('kmn/theme/') === 0) console.log('[theme]', e.data.type, e.data.tokens ? 'tokens' : ''); });`
    3. Navigate to `/umsatz-intelligenz` — expect `[theme] kmn/theme/request` then `[theme] kmn/theme/set tokens`.
    4. Navigate to `/tickets`.
    5. Navigate back to `/umsatz-intelligenz`.
    6. PASS CONDITION: second `[theme] kmn/theme/set tokens` appears; widget re-renders cleanly without stale tokens. Repeat 2× more — publisher survives.

    **UAT-7 (optional, diagnostic) — McpErrorBoundary forced throw (PORT-05):**
    1. If practical: temporarily edit widget-side `App.tsx` to `throw new Error('test')` inside the render path on condition `window.location.search.includes('force-throw')`. Push to mcp-poc staging.
    2. Open `https://staging.portal.kamanin.at/umsatz-intelligenz?force-throw`.
    3. PASS CONDITION: McpErrorBoundary renders a German error screen with a reload button. Clicking reload dismisses the boundary (or reloads the page).
    4. Revert the throw edit. Redeploy.

    **Recording protocol:**
    For each UAT item, record in the checkpoint resume-signal message:
    - UAT-1: median ms + PASS/FAIL
    - UAT-2: 4 screenshots (09:00/11:00/14:00/17:00) + pace indicator state per render + PASS/FAIL, OR "DDEV unavailable, deferring to widget unit tests"
    - UAT-3: which block was sabotaged + screenshot of mixed state + PASS/FAIL
    - UAT-4: two screenshots (mock=basket-aov + mock=null) + PASS/FAIL
    - UAT-5: network log excerpt showing `weekly_heatmap` payload + PASS/FAIL
    - UAT-6: console log of kmn/theme/set count + PASS/FAIL
    - UAT-7: screenshot of forced-throw error screen + PASS/FAIL, OR "deferred"
  </how-to-verify>
  <resume-signal>
    Reply with the UAT results in the format:
      "UAT-1: {ms} {pass|fail}
       UAT-2: {pass|fail|dev-env-unavailable}
       UAT-3: {pass|fail}
       UAT-4: {pass|fail}
       UAT-5: {pass|fail}
       UAT-6 (optional): {pass|fail|skipped}
       UAT-7 (optional): {pass|fail|skipped}"

    If any required UAT (1-5) fails, describe the failure and which plan should be revisited. If all required UATs pass, reply "approved".
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI run → portal repo | Task 1 runs read-only git commands; no writes |
| Human verifier → staging portal | UAT performed against live staging deployment; no production data touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08-01 | Information Disclosure | UAT sabotage of upstream ability leaks error detail to screenshot | accept | Block-level error skeleton renders hardcoded copy; no server error.message surfaced (plan 01 + 07 grep acceptance). Screenshot captures only the hardcoded `Daten nicht verfügbar` text. |
| T-19-08-02 | Tampering | Human verifier edits widget code to force-throw (UAT-7) and forgets to revert | mitigate | UAT-7 instructions explicitly include "Revert the throw edit. Redeploy." as step 4. Re-run UAT-7 post-revert to confirm forced-throw path is disarmed. |
| T-19-08-03 | Integrity | Period-toggle request bypasses mcp-proxy allowlist | mitigate | Tool name is `weekly_heatmap` (in ALLOWED_TOOLS since Phase 17-05); mcp-proxy EF rejects any other name. Plan 05 enforces widget-side via grep + test case. |
</threat_model>

<verification>
Wave 3 plan 08 complete when:
- Task 1 automated verifications all pass (PORT-02 ≥ 2 grep, PORT-03 ≥ 1 useMemo, PORT-04 zero diff, PORT-05 German ≥ 2 + reload ≥ 1 + English === 0)
- Task 2 checkpoint returns "approved" with all required UAT-1 through UAT-5 marked pass (or with documented deferrals for DDEV-dependent UAT-2)
- No PORTAL code changes made during Phase 19 execution (PORT-04 hard constraint)
- Phase 19 is ready for `/gsd-verify-work` to close the phase
</verification>

<success_criteria>
- 4 PORT-* verifications automated and green
- 5 required UAT items verified on staging (UAT-1 latency ≤ 2s, UAT-2 -85% bug non-reproducible, UAT-3 per-block error isolation, UAT-4 aov_bands fixture mode, UAT-5 period-toggle end-to-end)
- 2 optional UAT items (UAT-6 theme publisher remount, UAT-7 McpErrorBoundary forced throw) performed if time permits; failures do not block phase close but are logged for Phase 19.1
- Phase 19 delivers the 4-block dashboard, eliminates the -85% bug, ships at ≤ 300 KB gz, keeps PORTAL at zero TS diff, and passes human UAT — Milestone v3.0 Success Criterion #1 + #6 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/19-revenue-intelligence-widget-v2/19-08-SUMMARY.md` documenting:
- Task 1 verification matrix (4 PORT-* checks + numeric evidence)
- Task 2 UAT results (5 required + 2 optional, with screenshots referenced or inline notes)
- Any deferred items with routing (e.g. "UAT-2 deferred because Summerfield DDEV not reachable from build host — widget-side unit tests cover the confidence-branch logic that eliminates the bug")
- Phase 19 final status: READY FOR /gsd-verify-work (or BLOCKED ON: <item>)
</output>
