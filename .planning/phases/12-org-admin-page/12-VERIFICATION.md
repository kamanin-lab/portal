---
phase: 12-org-admin-page
verified: 2026-04-15T13:10:00Z
status: human_needed
score: 6/6
overrides_applied: 0
human_verification:
  - test: "Log in to staging as an admin user and navigate to /organisation. Verify OrgInfoSection shows org name, slug, and credit balance. Verify TeamSection shows the full member table with Name, E-Mail, Rolle, Hinzugefügt am columns."
    expected: "Page renders OrgInfoSection and TeamSection with live data from org_members + profiles join."
    why_human: "Data-flow from DB through RLS admin policy to UI requires a real session with actual org_members rows — cannot verify programmatically without a live admin session."
  - test: "Click 'Mitglied einladen', enter a fresh email address and select a role. Submit the form."
    expected: "Success toast appears. New row shows 'Einladung ausstehend' in the Name column. invite-member Edge Function called with correct payload."
    why_human: "End-to-end invite flow requires live Edge Function, Supabase auth user creation, and email delivery via Mailjet — not testable in unit tests."
  - test: "Click ··· on a non-admin member row. Select 'Rolle ändern'. Then select 'Entfernen' on another row and confirm the dialog."
    expected: "Role change: row updates immediately, success toast shows. Removal: row disappears, success toast shows. Admin's own row and other admin rows have no ··· button."
    why_human: "Direct Supabase writes via RLS admin policies (Plan 12-01) require a live admin session. Cannot verify guard behavior without real org_members data."
  - test: "Visit /passwort-setzen directly without a session (no invite link). Then open an invite email and click the link."
    expected: "Direct visit shows 'Link abgelaufen' card with link to /login. Invite link opens /passwort-setzen with password form. Setting password navigates to /tickets."
    why_human: "GoTrue session hydration from URL hash requires a real recovery token generated by the invite-member Edge Function — cannot simulate in unit tests."
---

# Phase 12: org-admin-page Verification Report

**Phase Goal:** Organisation admins can manage their team from a dedicated portal page — inviting new members, changing roles, and removing members — while invited users can set their password via the invite landing page

**Verified:** 2026-04-15T13:10:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Navigating to `/organisation` as an admin renders the org info section (name, slug, credit info) and the full team member table (name, email, role, joined date) | VERIFIED | `OrgInfoSection.tsx` reads `useOrg()` + `useCredits()`; `TeamSection.tsx` fetches via `useOrgMembers()` with embedded `profile:profiles` join; 5-column grid (name, email, role, joined, actions); 11 tests pass |
| 2 | Non-admin users are immediately redirected away from `/organisation` — the page is never rendered for them | VERIFIED | `OrganisationPage.tsx` line 11: `if (!isAdmin && !isLoading) return <Navigate to="/tickets" replace />` |
| 3 | Admin submitting invite dialog triggers invite flow; new member appears as "Einladung ausstehend"; success toast shown | VERIFIED | `InviteMemberDialog.tsx` calls `supabase.functions.invoke('invite-member', {...})`, `queryClient.invalidateQueries(['org-members'])` on success, `toast.success`; `TeamSection.tsx` detects `profile.full_name === null` and renders `<em>Einladung ausstehend</em>`; 3 tests pass |
| 4 | Admin can change a member's role (Mitglied ↔ Betrachter) and cannot demote themselves or last admin | VERIFIED | `useMemberActions.ts` contains `Cannot demote last admin` guard + `supabase.from('org_members').update({ role: nextRole }).eq('id', memberId)`; `MemberRowActions.tsx` returns null for self/admin rows at UI level; 4 hook tests + 4 component tests pass |
| 5 | Admin can remove a member after confirming dialog; cannot remove self if last admin | VERIFIED | `useMemberActions.ts` contains self-as-last-admin guard + `supabase.from('org_members').delete().eq('id', memberId)`; `MemberRowActions.tsx` wires `ConfirmDialog` with `destructive={true}` |
| 6 | Newly invited user opening invite link lands on `/passwort-setzen`, sets password, redirected to `/tickets` | VERIFIED | `PasswortSetzenPage.tsx` reads `useAuth().session`; expired-link state when no session; calls `updatePassword(password)` then `navigate('/tickets', { replace: true })`; route at line 40 (before ProtectedRoute at line 42); 5 tests pass |

**Score:** 6/6 truths verified (automated checks)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `supabase/migrations/20260416120000_org_admin_write_rls.sql` | 3 RLS policies (admin SELECT, UPDATE, DELETE on org_members) | VERIFIED | 3 `create policy` + 3 `drop policy if exists`; 4 occurrences of `user_org_role(organization_id) = 'admin'` |
| `src/modules/organisation/hooks/useOrgMembers.ts` | React Query hook, keyed ['org-members', org.id], profiles join | VERIFIED | `queryKey: ['org-members', organization?.id]`; `profile:profiles(id, email, full_name)`; `staleTime: 2 * 60 * 1000`; `enabled: !!organization?.id` |
| `src/modules/organisation/hooks/useMemberActions.ts` | Mutations: changeRole + removeMember with guards | VERIFIED | 55 lines; both guards present; `queryClient.invalidateQueries({ queryKey: ['org-members'] })` after each mutation |
| `src/modules/organisation/pages/OrganisationPage.tsx` | Admin page, ContentContainer narrow, guard, renders sections | VERIFIED | 28 lines; `width="narrow"`; `<Navigate to="/tickets" replace />`; renders `<OrgInfoSection />` + `<TeamSection />` |
| `src/modules/organisation/components/OrgInfoSection.tsx` | Read-only org info card | VERIFIED | 38 lines; `bg-surface rounded-[14px] border border-border p-5`; reads `useOrg()` + `useCredits()` |
| `src/modules/organisation/components/TeamSection.tsx` | Member table + invite button + 5-column grid | VERIFIED | 72 lines; `grid-cols-[1fr_1fr_120px_100px_40px]`; "Mitglied einladen"; "Einladung ausstehend"; "Noch keine Mitglieder"; renders `<MemberRowActions />` |
| `src/modules/organisation/components/InviteMemberDialog.tsx` | Modal dialog posting to invite-member Edge Function | VERIFIED | 93 lines; `supabase.functions.invoke('invite-member'`; `organizationId: organization.id`; `queryClient.invalidateQueries({ queryKey: ['org-members'] })` |
| `src/modules/organisation/components/MemberRowActions.tsx` | Radix DropdownMenu, ConfirmDialog, self/admin guards | VERIFIED | 76 lines; `DropdownMenu.Root`; "Rolle ändern"; "Entfernen"; `ConfirmDialog`; `if (member.profile_id === user?.id) return null` |
| `src/shared/pages/PasswortSetzenPage.tsx` | Password-set page outside ProtectedRoute | VERIFIED | 112 lines; `updatePassword(password)`; `navigate('/tickets', { replace: true })`; "Link abgelaufen"; "Passwort festlegen" |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| Sidebar "Ihre Organisation" link | /organisation route | NavLink `to="/organisation"` | VERIFIED | `SidebarUtilities.tsx` line 55: `{isAdmin && (` + `to="/organisation"` |
| OrganisationPage admin guard | /tickets redirect | `<Navigate to="/tickets" replace />` | VERIFIED | `OrganisationPage.tsx` line 11 |
| useAuth PASSWORD_RECOVERY handler | /passwort-setzen | `window.location.href = '/passwort-setzen'` | VERIFIED | `useAuth.ts` line 112; line 179 (voluntary reset) retains `/konto?action=change-password` — correct by design |
| InviteMemberDialog submit | invite-member Edge Function | `supabase.functions.invoke('invite-member'` | VERIFIED | `InviteMemberDialog.tsx` line 35 |
| MemberRowActions "Rolle ändern" | useMemberActions.changeRole | `changeRole({ memberId, nextRole })` | VERIFIED | `MemberRowActions.tsx` line 47 |
| MemberRowActions "Entfernen" | ConfirmDialog + removeMember | `setRemoveOpen(true)` + `removeMember` on confirm | VERIFIED | `MemberRowActions.tsx` — ConfirmDialog with `onConfirm` calling `removeMember` |
| useMemberActions role change | org_members UPDATE | `.update({ role: nextRole }).eq('id', memberId)` | VERIFIED | `useMemberActions.ts` line 25 |
| PasswortSetzenPage submit | GoTrue updateUser | `updatePassword(password)` | VERIFIED | `PasswortSetzenPage.tsx` line 47 |
| Successful password set | /tickets | `navigate('/tickets', { replace: true })` | VERIFIED | `PasswortSetzenPage.tsx` line 53 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| OrgInfoSection | `organization` (name, slug) | `useOrg()` → OrgContext → Supabase `organizations` query | Yes — live DB query | FLOWING |
| OrgInfoSection | `credits.balance` | `useCredits()` → `get_org_credit_balance` RPC | Yes — RPC returns real balance | FLOWING |
| TeamSection | `members[]` | `useOrgMembers()` → Supabase `org_members` + `profiles` join | Yes — live DB query; admin RLS (Plan 12-01) authorizes full org read | FLOWING |
| useMemberActions | writes to `org_members` | Direct Supabase UPDATE/DELETE via admin RLS | Yes — actual DB write; admin RLS applied at DB layer | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Build produces OrganisationPage bundle | `npm run build` | `OrganisationPage-DDfuAeps.js` in dist/assets | PASS |
| Build produces PasswortSetzenPage bundle | `npm run build` | `PasswortSetzenPage-BAI1w8hu.js` in dist/assets | PASS |
| All Phase 12 unit tests pass | `npx vitest run src/modules/organisation src/shared/__tests__/PasswortSetzenPage.test.tsx` | 6 test files, 24 tests passed | PASS |
| /passwort-setzen is before ProtectedRoute | grep in routes.tsx | Line 40 (/passwort-setzen) before line 42 (ProtectedRoute) | PASS |
| Migration has exactly 3 create policy statements | grep count | 3 matches | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ORG-FE-UI-01 | 12-02 | `/organisation` route; non-admin redirect to `/tickets` | SATISFIED | Route at line 64 inside ProtectedRoute; admin guard in OrganisationPage; REQUIREMENTS.md marked [x] |
| ORG-FE-UI-02 | 12-02, 12-03 | OrgInfoSection + TeamSection rendered in OrganisationPage | SATISFIED | OrganisationPage imports and renders both; REQUIREMENTS.md marked [x] |
| ORG-FE-UI-03 | 12-03 | TeamSection table columns; "Einladung ausstehend" for pending | SATISFIED | TeamSection 5-column grid + pending detection; REQUIREMENTS.md marked [x] |
| ORG-FE-UI-04 | 12-03 | InviteMemberDialog: email + role; invite-member Edge Function; toasts | SATISFIED | InviteMemberDialog confirmed; REQUIREMENTS.md marked [x] |
| ORG-FE-UI-05 | 12-04 | Role change Mitglied↔Betrachter; cannot demote self/last admin | SATISFIED | useMemberActions guards confirmed; REQUIREMENTS.md marked [x] |
| ORG-FE-UI-06 | 12-04 | Remove member; cannot remove self-as-last-admin; ConfirmDialog | SATISFIED | useMemberActions + MemberRowActions + ConfirmDialog confirmed; REQUIREMENTS.md marked [x] |
| ORG-FE-UI-07 | 12-05 | `/passwort-setzen` page; GoTrue session; updateUser; redirect /tickets | SATISFIED | PasswortSetzenPage confirmed; REQUIREMENTS.md marked [x] |
| ORG-FE-UI-08 | 12-02 | Sidebar "Ihre Organisation" link (admin-only, Hugeicons building icon) | SATISFIED | SidebarUtilities.tsx: `Building05Icon`, `{isAdmin && (`, "Ihre Organisation"; REQUIREMENTS.md marked [x] |

**All 8 requirements: SATISFIED**

---

### Anti-Patterns Found

No blockers or stubs detected in Phase 12 files. All components read live data from hooks. No hardcoded empty arrays or placeholder text flow to user-visible output.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| No issues found | — | — | — | — |

---

### Pre-Existing Test Failures (Not Phase 12 Regressions)

2 test files with 5 failures were present before Phase 12 and are unrelated to this phase:

| Test File | Failures | Last Modified By | Impact |
|-----------|----------|-----------------|--------|
| `src/modules/tickets/__tests__/task-list-utils.test.ts` | 1 (filter "attention") | Phase 8 — `95435d6` | Pre-existing; Phase 12 did not touch `task-list-utils.ts` |
| `src/shared/pages/__tests__/MeineAufgabenPage.test.tsx` | 4 (4-tab filter) | Phase 8 — `433531b` | Pre-existing; confirmed in 12-05 SUMMARY as "pre-existing, out of scope" |

These failures are not introduced by Phase 12. The 24 Phase 12 tests all pass cleanly.

---

### Human Verification Required

#### 1. Admin org page — live data rendering

**Test:** Log in to staging.portal.kamanin.at as an admin user. Navigate to `/organisation` via the sidebar "Ihre Organisation" link.

**Expected:**
- "Ihre Organisation" link appears in the sidebar Utilities zone (below Hilfe)
- Page renders OrgInfoSection card with real org name, slug, and credit balance
- TeamSection shows member table with all columns (Name, E-Mail, Rolle, Hinzugefügt am)
- Non-admin members show ··· button; own row and admin rows do NOT

**Why human:** RLS admin policies (Plan 12-01) are staging-only. Live DB query through admin RLS + GoTrue session required to confirm data actually flows.

---

#### 2. Invite member end-to-end

**Test:** On the /organisation page as admin, click "Mitglied einladen". Enter a fresh email address (not existing member). Select role "Mitglied". Click "Einladen".

**Expected:**
- Success toast: "Einladung gesendet."
- New row appears in TeamSection with "Einladung ausstehend" in Name column and the entered email
- invite-member Edge Function triggered (check Supabase Edge Function logs if needed)

**Why human:** Requires live invite-member Edge Function, Mailjet email delivery, and real org_members row insertion — not verifiable in unit tests.

---

#### 3. Role change and member removal

**Test:** On a non-admin member row (not own row), click ···. First try "Rolle ändern". Then try "Entfernen" on a different member and cancel, then confirm.

**Expected:**
- "Rolle ändern": row role column updates, success toast shown
- "Entfernen" cancel: dialog closes, no change
- "Entfernen" confirm: row removed, success toast shown
- Attempting to demote self or last admin shows error toast (guard fires)

**Why human:** Requires live admin RLS UPDATE/DELETE authorization from Plan 12-01 staging migration.

---

#### 4. Invite landing page (/passwort-setzen) end-to-end

**Test A (expired):** Visit https://staging.portal.kamanin.at/passwort-setzen directly without clicking an invite link.

**Expected:** "Link abgelaufen" card shown with "Zur Anmeldung" link back to /login.

**Test B (active):** Open the invite email sent in step 2 above and click the link.

**Expected:** /passwort-setzen loads with password form ("Neues Passwort", "Passwort bestätigen"). Enter matching passwords (min 8 chars) and submit. User is redirected to /tickets and can use the portal.

**Why human:** GoTrue recovery token in URL hash requires a real invite email — cannot simulate without a live Supabase session.

---

### Gaps Summary

No gaps identified. All 6 roadmap success criteria are verified by code inspection, unit tests (24/24 pass), and build output. The 4 human verification items are standard integration-level checks required for any live auth + DB flow — they do not indicate missing implementation.

---

## Verdict

**Phase 12 is COMPLETE at the implementation level.**

All 5 plans executed and documented:
- Plan 12-01: Migration file with 3 admin RLS policies — DONE
- Plan 12-02: useAuth fix + useOrgMembers hook + OrganisationPage shell + route + sidebar — DONE
- Plan 12-03: OrgInfoSection + TeamSection + InviteMemberDialog wired into OrganisationPage — DONE
- Plan 12-04: useMemberActions + MemberRowActions + TeamSection 5-column update — DONE
- Plan 12-05: PasswortSetzenPage + public /passwort-setzen route — DONE

**Pending:** Human staging verification of live data flow and invite end-to-end (4 items above).

---

_Verified: 2026-04-15T13:10:00Z_
_Verifier: Claude (gsd-verifier)_
