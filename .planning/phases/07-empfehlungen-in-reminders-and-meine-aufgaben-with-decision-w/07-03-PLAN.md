---
phase: 07
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/shared/pages/MeineAufgabenPage.tsx
  - src/modules/tickets/components/RecommendationsBlock.tsx
  - src/modules/tickets/components/RecommendationCard.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "MeineAufgabenPage renders RecommendationsBlock when recommendations exist"
    - "Empty state only shows when both attentionTasks and recommendations are empty"
    - "Clicking a recommendation opens TaskDetailSheet via setSearchParams(taskId)"
    - "TaskDetailSheet's existing RecommendationApproval (Ja/Nein) workflow works unchanged"
    - "'Später' button on RecommendationCard hides the recommendation in the current session (no backend write)"
    - "Session-snoozed recommendations are excluded from BOTH the rendered block AND the empty-state recommendation count"
    - "Page stays under 150 lines total"
  artifacts:
    - path: "src/shared/pages/MeineAufgabenPage.tsx"
      provides: "Meine Aufgaben UI with recommendations block + session snooze"
      contains: "useRecommendations"
      contains_also: "RecommendationsBlock"
  key_links:
    - from: "src/shared/pages/MeineAufgabenPage.tsx"
      to: "src/modules/tickets/hooks/useRecommendations.ts"
      via: "hook call"
      pattern: "useRecommendations\\(tasks\\)"
    - from: "src/shared/pages/MeineAufgabenPage.tsx"
      to: "src/modules/tickets/components/RecommendationsBlock.tsx"
      via: "component import + render"
      pattern: "<RecommendationsBlock"
    - from: "RecommendationsBlock card click"
      to: "TaskDetailSheet"
      via: "openTask → setSearchParams(taskId)"
      pattern: "onTaskClick=\\{openTask\\}"
---

<objective>
Add the Empfehlungen (recommendations) block to `MeineAufgabenPage`. Clicking a recommendation card opens the existing `TaskDetailSheet`, which already renders `RecommendationApproval` (Ja/Nein). Add a session-only "Später" (defer) action that hides the recommendation until the next page load.

Purpose: Close Gap 2 and Gap 3 from RESEARCH.md — make MeineAufgabenPage the decision hub for recommendations.
Output: Modified `MeineAufgabenPage.tsx` with recommendations block and session-level snooze; Wave 0 tests from 07-01 Task 2 (MeineAufgabenPage.test.tsx) and 07-01 Task 4 (RecommendationsBlock.test.tsx) turn GREEN; human-verify checkpoint confirms the flow end-to-end.

Note: Plan is `autonomous: true`. The `checkpoint:human-verify` task at the end gates execution at the correct point — the implementation task (Task 1) runs without prompting, and the executor pauses only at Task 2 for human approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-RESEARCH.md
@src/shared/pages/MeineAufgabenPage.tsx
@src/modules/tickets/hooks/useRecommendations.ts
@src/modules/tickets/components/RecommendationsBlock.tsx
@src/modules/tickets/components/RecommendationCard.tsx
@src/modules/tickets/pages/TicketsPage.tsx
@.planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-01-PLAN.md

<interfaces>
From src/modules/tickets/hooks/useRecommendations.ts:
```typescript
export function useRecommendations(tasks: ClickUpTask[]): {
  recommendations: ClickUpTask[]
  count: number
}
```

Expected RecommendationsBlock props (verify against actual file; extend if onSnooze is missing):
```typescript
interface RecommendationsBlockProps {
  recommendations: ClickUpTask[]
  onTaskClick: (id: string) => void
  onSnooze?: (id: string) => void  // NEW — session-only defer
}
```

RecommendationCard props (verify and extend if needed):
```typescript
interface RecommendationCardProps {
  task: ClickUpTask
  onClick?: (id: string) => void
  onSnooze?: (id: string) => void  // NEW — renders "Später" button when present
}
```

MeineAufgabenPage openTask function (already exists):
```typescript
function openTask(id: string) {
  setSearchParams({ taskId: id }, { replace: true })
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate recommendations + session snooze into MeineAufgabenPage (GREEN for 07-01 Tasks 2 + 4)</name>
  <files>
    src/shared/pages/MeineAufgabenPage.tsx,
    src/modules/tickets/components/RecommendationsBlock.tsx,
    src/modules/tickets/components/RecommendationCard.tsx
  </files>
  <read_first>
    - src/shared/pages/MeineAufgabenPage.tsx (current 123-line file — must stay < 150 lines)
    - src/shared/pages/__tests__/MeineAufgabenPage.test.tsx (RED tests from 07-01 Task 2 — must turn GREEN)
    - src/modules/tickets/__tests__/RecommendationsBlock.test.tsx (RED tests from 07-01 Task 4 — must turn GREEN)
    - src/modules/tickets/hooks/useRecommendations.ts
    - src/modules/tickets/components/RecommendationsBlock.tsx (full file — check if onSnooze prop already exists)
    - src/modules/tickets/components/RecommendationCard.tsx (full file — check if onSnooze prop already exists)
    - src/modules/tickets/pages/TicketsPage.tsx (current RecommendationsBlock usage as reference)
  </read_first>
  <behavior>
    This task turns all Wave 0 RED tests GREEN. It introduces NO new tests — it only makes existing stubs pass.

    From `src/shared/pages/__tests__/MeineAufgabenPage.test.tsx` (Plan 01 Task 2):
    - Test 1 (UI-01): MeineAufgabenPage renders RecommendationsBlock with recommendation task names.
    - Test 2 (UI-02 positive): Empty state shows only when BOTH attentionTasks and recommendations are empty.
    - Test 3 (UI-02 negative): Empty state is NOT shown when recommendations exist.
    - Test 4 (UI-03): Clicking a recommendation card triggers setSearchParams({ taskId }).

    From `src/modules/tickets/__tests__/RecommendationsBlock.test.tsx` (Plan 01 Task 4):
    - Test A (UI-02 Später): Clicking "Später" on a card calls `onSnooze(clickup_id)` and does NOT trigger `onTaskClick` (stopPropagation).
    - Test B (UI-02 Später backward-compat): When `onSnooze` is undefined, the "Später" button is NOT rendered.

    Additional page-level behavior (not in Wave 0 tests, but verified in the human checkpoint Task 2):
    - Snoozing a recommendation on MeineAufgabenPage removes it from the rendered list for the current session; remounting the page brings it back.
  </behavior>
  <action>
    1. **MeineAufgabenPage.tsx:**
       - Add imports:
         ```typescript
         import { useState, useMemo } from 'react'
         import { useRecommendations } from '@/modules/tickets/hooks/useRecommendations'
         import { RecommendationsBlock } from '@/modules/tickets/components/RecommendationsBlock'
         ```
       - After the existing `const { taskUnread } = useUnreadCounts(user?.id)` line, add:
         ```typescript
         const [snoozedIds, setSnoozedIds] = useState<Set<string>>(new Set())
         const { recommendations: allRecommendations } = useRecommendations(tasks)
         const recommendations = useMemo(
           () => allRecommendations.filter(r => !snoozedIds.has(r.clickup_id)),
           [allRecommendations, snoozedIds],
         )
         const snoozeRecommendation = (id: string) => {
           setSnoozedIds(prev => { const next = new Set(prev); next.add(id); return next })
         }
         ```
       - Update the empty state condition:
         ```tsx
         {!isLoading && attentionTasks.length === 0 && recommendations.length === 0 && ( ... )}
         ```
       - Update the page subtitle to `"Aufgaben und Empfehlungen, die Ihre Entscheidung erfordern"`.
       - Update the header count badge to show `attentionTasks.length + recommendations.length` when > 0.
       - After the grouped task list (`[...grouped.entries()].map(...)`), before `<TaskDetailSheet ...>`, render:
         ```tsx
         {!isLoading && recommendations.length > 0 && (
           <RecommendationsBlock
             recommendations={recommendations}
             onTaskClick={openTask}
             onSnooze={snoozeRecommendation}
           />
         )}
         ```
       - VERIFY total line count remains < 150 (`wc -l src/shared/pages/MeineAufgabenPage.tsx`). If over budget, inline the badge ternary or extract the snooze logic into a small `useSessionSnooze` helper in the same file — do NOT split into a new file.

    2. **RecommendationsBlock.tsx:** If `onSnooze?: (id: string) => void` is not in its props, add it and forward to each `RecommendationCard`. Do not change the existing behavior when `onSnooze` is undefined (TicketsPage callers remain unaffected — existing call signature still works).

    3. **RecommendationCard.tsx:** If `onSnooze?: (id: string) => void` is not a prop, add it. When present, render a small secondary button labelled `"Später"` next to (or beneath) the existing click target. Use shadcn/ui `Button` variant `ghost` size `sm` with German label `"Später"`. Clicking it calls `onSnooze(task.clickup_id)` and MUST `event.stopPropagation()` so the card click (which opens the sheet) is NOT triggered. When `onSnooze` is undefined, render nothing — existing TicketsPage usage unchanged.

    4. Do NOT introduce any backend changes. Do NOT add a new Edge Function action. Do NOT touch `useTaskActions`, `update-task-status`, or any hook.

    5. Keep all user-facing strings in German. Do NOT use Lucide icons — if an icon is needed for the "Später" button use `@hugeicons/react` with a clock-like icon (e.g., `Clock01Icon` from `@hugeicons/core-free-icons`).
  </action>
  <verify>
    <automated>npm run test -- MeineAufgabenPage --run &amp;&amp; npm run test -- RecommendationsBlock --run &amp;&amp; test $(wc -l &lt; src/shared/pages/MeineAufgabenPage.tsx) -lt 150</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "useRecommendations" src/shared/pages/MeineAufgabenPage.tsx`
    - `grep -q "RecommendationsBlock" src/shared/pages/MeineAufgabenPage.tsx`
    - `grep -q "snoozedIds" src/shared/pages/MeineAufgabenPage.tsx`
    - `grep -q "Aufgaben und Empfehlungen" src/shared/pages/MeineAufgabenPage.tsx`
    - `wc -l < src/shared/pages/MeineAufgabenPage.tsx` returns a number LESS than 150
    - `grep -q "onSnooze" src/modules/tickets/components/RecommendationsBlock.tsx`
    - `grep -q "Später" src/modules/tickets/components/RecommendationCard.tsx`
    - `grep -q "stopPropagation" src/modules/tickets/components/RecommendationCard.tsx` (new snooze button handler)
    - `npm run test -- MeineAufgabenPage --run` exits 0 (all 4 Wave 0 tests GREEN)
    - `npm run test -- RecommendationsBlock --run` exits 0 (both Wave 0 Später tests GREEN)
    - `npm run lint` exits 0 — no new lint errors introduced
    - `npm run build` exits 0 — TypeScript compiles
    - No Lucide imports introduced (`grep -r "from 'lucide-react'" src/shared/pages/MeineAufgabenPage.tsx src/modules/tickets/components/RecommendationCard.tsx src/modules/tickets/components/RecommendationsBlock.tsx` returns nothing)
    - Existing TicketsPage tests still GREEN — `npm run test -- TicketsPage --run` exits 0 (no regression from prop-optional changes to RecommendationsBlock/Card)
  </acceptance_criteria>
  <done>MeineAufgabenPage renders recommendations, clicking opens sheet with Ja/Nein, Später hides for session, all Wave 0 tests green, linecount compliant.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify Meine Aufgaben recommendations flow</name>
  <files>src/shared/pages/MeineAufgabenPage.tsx</files>
  <read_first>
    - .planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-03-PLAN.md (this plan's Task 1 acceptance criteria)
  </read_first>
  <action>
    Human verification only. The executor pauses here and prints the `<how-to-verify>` steps to the user. No file modifications happen in this task — the user manually walks through the UI and either approves or reports issues.

    Verification steps for the user:
    1. Start dev server: `npm run dev`
    2. Log in with test account that has at least one pending recommendation task (tag=recommendation, status=to do). If none exist on staging, create one: manually tag a test task in ClickUp with `recommendation` and set status to `to do`.
    3. Navigate to `/meine-aufgaben` (sidebar: "Meine Aufgaben").
    4. Expect: Below any attention tasks you see a "Empfehlungen" section with one or more cards.
    5. Click a recommendation card → TaskDetailSheet opens on the right → scroll to bottom → verify `RecommendationApproval` panel shows with German "Annehmen" and "Ablehnen" buttons.
    6. Close the sheet (X or backdrop click). Recommendation still visible on the page.
    7. Click the "Später" button on the same card → card disappears immediately without a network request (check DevTools Network tab — no POST).
    8. Refresh the page (F5) → the "snoozed" recommendation reappears (session-only behavior).
    9. Verify empty state: if the test profile happens to have ZERO attention tasks AND ZERO visible recommendations, the "Keine offenen Aufgaben — alles erledigt!" empty state should display. If there are visible recommendations but zero attention tasks, the empty state MUST NOT show.
    10. Verify German text only — no English strings anywhere in the section.
    11. Verify page subtitle now reads `"Aufgaben und Empfehlungen, die Ihre Entscheidung erfordern"`.
  </action>
  <what-built>
    MeineAufgabenPage now shows an Empfehlungen section below the attention task list. Clicking a recommendation opens the TaskDetailSheet with the existing Ja/Nein (Annehmen / Ablehnen) RecommendationApproval UI. A "Später" button on each recommendation card hides it for the current session only.
  </what-built>
  <how-to-verify>
    See `<action>` — the 11 steps listed there are the verification script. Execute them manually against `npm run dev`.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — awaiting human approval"</automated>
  </verify>
  <acceptance_criteria>
    - User types "approved" after walking through all 11 steps
    - OR user reports specific issues and execution loops back to Task 1
  </acceptance_criteria>
  <done>User explicitly types "approved" — phase 7 complete.</done>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Supabase | Pure frontend change; no new network calls introduced |
| user → session state | `snoozedIds` is in-memory React state, never persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Information Disclosure | Recommendations render from cached tasks | mitigate | `useClickUpTasks` already filters by RLS (profile_id) at query level — no change to data access |
| T-07-11 | Tampering | User bypasses snooze by clearing state | accept | Snooze is explicitly session-only by design; no security claim — just a UX convenience |
| T-07-12 | Spoofing | onTaskClick opens arbitrary taskId | mitigate | `openTask` only sets the URL param; TaskDetailSheet still looks up the task via `tasks` array which is RLS-filtered |
| T-07-13 | Elevation of Privilege | Clicking Später triggers hidden mutation | mitigate | Verify via DevTools Network tab in checkpoint Task 2 that no request is issued on Später click |
</threat_model>

<verification>
- `MeineAufgabenPage.test.tsx` — 4 tests GREEN
- `RecommendationsBlock.test.tsx` — 2 Später tests GREEN
- `npm run test` — full suite no new failures
- `npm run build` — TypeScript clean
- `npm run lint` — no new errors
- `wc -l src/shared/pages/MeineAufgabenPage.tsx` < 150
- Human checkpoint approved
</verification>

<success_criteria>
- Client on MeineAufgabenPage sees pending recommendations
- Clicking a recommendation opens the existing Ja/Nein decision UI
- Später hides recommendation for the session without any backend write
- Page stays under 150-line limit
- No regressions in TicketsPage recommendations block
</success_criteria>

<output>
Create `.planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-03-SUMMARY.md`
</output>
