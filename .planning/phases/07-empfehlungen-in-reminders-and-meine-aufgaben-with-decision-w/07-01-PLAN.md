---
phase: 07
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260414100000_recommendation_reminder_column.sql
  - src/shared/pages/__tests__/MeineAufgabenPage.test.tsx
  - src/modules/tickets/__tests__/emailCopy-recommendation.test.ts
  - src/modules/tickets/__tests__/RecommendationsBlock.test.tsx
  - docs/system-context/DATABASE_SCHEMA.md
autonomous: true
requirements:
  - REMIND-01
  - REMIND-02
  - UI-01
  - UI-02
  - UI-03
  - EMAIL-01

must_haves:
  truths:
    - "profiles table has last_recommendation_reminder_sent_at column"
    - "Wave 0 failing tests exist for MeineAufgabenPage recommendations UI"
    - "Wave 0 failing test exists for RecommendationsBlock Später session-snooze"
    - "Wave 0 failing test exists for recommendation_reminder email copy"
  artifacts:
    - path: "supabase/migrations/20260414100000_recommendation_reminder_column.sql"
      provides: "DB column migration"
      contains: "last_recommendation_reminder_sent_at"
    - path: "src/shared/pages/__tests__/MeineAufgabenPage.test.tsx"
      provides: "Failing component stubs for REC block in MeineAufgabenPage"
    - path: "src/modules/tickets/__tests__/RecommendationsBlock.test.tsx"
      provides: "Failing stub for Später session-only hide behavior"
    - path: "src/modules/tickets/__tests__/emailCopy-recommendation.test.ts"
      provides: "Failing unit stub for recommendation_reminder email copy"
  key_links:
    - from: "supabase/migrations/20260414100000_recommendation_reminder_column.sql"
      to: "public.profiles"
      via: "ALTER TABLE ... ADD COLUMN"
      pattern: "ADD COLUMN.*last_recommendation_reminder_sent_at"
---

<objective>
Establish Wave 0 foundation for Phase 7: create the DB migration for the recommendation reminder cooldown column, apply it via `supabase db push`, and write failing test stubs for the MeineAufgabenPage recommendations block, the RecommendationsBlock Später session-snooze behavior, and the recommendation_reminder email copy entry.

Purpose: Nyquist compliance — downstream plans (02 backend, 03 frontend) require the DB column to exist and RED tests to drive TDD.
Output: Migration file applied to DB, three failing test files committed (vitest RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-RESEARCH.md
@.planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-VALIDATION.md
@supabase/migrations/20260414000000_add_unread_digest_timestamp.sql
@src/shared/pages/MeineAufgabenPage.tsx
@src/modules/tickets/components/RecommendationsBlock.tsx
@src/modules/tickets/components/RecommendationCard.tsx
@supabase/functions/_shared/emailCopy.ts

<interfaces>
<!-- MeineAufgabenPage current exports + key props needed by tests -->

From src/modules/tickets/components/RecommendationsBlock.tsx (expected props after Plan 03 extension):
```typescript
interface RecommendationsBlockProps {
  recommendations: ClickUpTask[]
  onTaskClick: (id: string) => void
  onSnooze?: (id: string) => void  // optional — absent in current file, added in Plan 03
}
```

From src/modules/tickets/hooks/useRecommendations.ts:
```typescript
export function useRecommendations(tasks: ClickUpTask[]): {
  recommendations: ClickUpTask[]
  count: number
}
```

From supabase/functions/_shared/emailCopy.ts:
- `EmailType` string union — must extend with `"recommendation_reminder"`
- `EMAIL_COPY` dict with per-locale `{ subject, title, greeting, body, cta, notes }`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration + schema push [BLOCKING]</name>
  <files>
    supabase/migrations/20260414100000_recommendation_reminder_column.sql,
    docs/system-context/DATABASE_SCHEMA.md
  </files>
  <read_first>
    - supabase/migrations/20260414000000_add_unread_digest_timestamp.sql (pattern template — mirror exactly)
    - docs/system-context/DATABASE_SCHEMA.md (profiles table section — add new column row)
    - .planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-RESEARCH.md (lines 170–174 for SQL spec)
  </read_first>
  <action>
    1. Create `supabase/migrations/20260414100000_recommendation_reminder_column.sql` with EXACTLY this content:
       ```sql
       -- Migration: add last_recommendation_reminder_sent_at for recommendation reminder cooldown
       ALTER TABLE public.profiles
         ADD COLUMN IF NOT EXISTS last_recommendation_reminder_sent_at timestamptz DEFAULT NULL;

       COMMENT ON COLUMN public.profiles.last_recommendation_reminder_sent_at
         IS 'Timestamp of last recommendation reminder email (5-day cooldown)';
       ```
    2. Apply migration to PRODUCTION self-hosted Supabase: `supabase db push` (from project root).
    3. Apply to STAGING Cloud Supabase (project ref `ahlthosftngdcryltapu`):
       - `supabase link --project-ref ahlthosftngdcryltapu`
       - `supabase db push`
       - `supabase link --project-ref <prod-ref>` (re-link to prod after staging push)
    4. Update `docs/system-context/DATABASE_SCHEMA.md`: in the profiles table section add row:
       `| last_recommendation_reminder_sent_at | timestamptz | NULL | Cooldown timestamp for recommendation_reminder emails (5-day) |`
       after the existing `last_unread_digest_sent_at` row (maintain alphabetical/chronological order if convention exists — otherwise append after `last_unread_digest_sent_at`).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260414100000_recommendation_reminder_column.sql &amp;&amp; grep -q "last_recommendation_reminder_sent_at" supabase/migrations/20260414100000_recommendation_reminder_column.sql</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at `supabase/migrations/20260414100000_recommendation_reminder_column.sql`
    - Migration contains `ADD COLUMN IF NOT EXISTS last_recommendation_reminder_sent_at timestamptz`
    - `supabase db push` exit code 0 on production
    - `supabase db push` exit code 0 on staging (ahlthosftngdcryltapu)
    - `grep "last_recommendation_reminder_sent_at" docs/system-context/DATABASE_SCHEMA.md` returns a line
    - Production re-link restored (`supabase projects list` shows prod marked as linked)
  </acceptance_criteria>
  <done>Column exists on both prod + staging, migration committed, schema doc updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wave 0 — MeineAufgabenPage test stubs (RED)</name>
  <files>src/shared/pages/__tests__/MeineAufgabenPage.test.tsx</files>
  <read_first>
    - src/shared/pages/MeineAufgabenPage.tsx (current state — does NOT render RecommendationsBlock)
    - src/modules/tickets/hooks/useRecommendations.ts
    - src/modules/tickets/components/RecommendationsBlock.tsx
    - src/modules/tickets/__tests__ (any existing test showing how they mock useClickUpTasks + QueryClientProvider wrapper)
    - CLAUDE.md (Testing section — mock patterns, QueryClientProvider wrapper)
  </read_first>
  <behavior>
    Test 1 (UI-01): Given tasks include a recommendation (status='to do', tags=[{name:'recommendation'}]), MeineAufgabenPage renders an element containing the recommendation's task name inside a region derived from RecommendationsBlock.
    Test 2 (UI-02 positive): Given `attentionTasks.length === 0` AND `recommendations.length === 0`, the empty state text "Keine offenen Aufgaben — alles erledigt!" is shown.
    Test 3 (UI-02 negative): Given `attentionTasks.length === 0` but `recommendations.length > 0`, the empty state text is NOT shown.
    Test 4 (UI-03): Clicking a recommendation card invokes setSearchParams with `taskId` equal to the recommendation's clickup_id (i.e. openTask wired through onTaskClick).

    NOTE: Später snooze behavior is covered in a separate Wave 0 stub file (see Task 4 — RecommendationsBlock.test.tsx). This file only covers page-level integration (UI-01, UI-02, UI-03).
  </behavior>
  <action>
    Create `src/shared/pages/__tests__/MeineAufgabenPage.test.tsx` as a RED-state test file:
    1. Use vitest + @testing-library/react.
    2. Wrap renders in QueryClientProvider (QueryClient with `retry: false`).
    3. Mock `@/modules/tickets/hooks/useClickUpTasks` to return a controllable `data` array.
    4. Mock `@/shared/hooks/useAuth` to return `{ user: { id: 'test-user' } }`.
    5. Mock `@/modules/tickets/hooks/useUnreadCounts` to return `{ taskUnread: {} }`.
    6. Use `vi.mock('react-router-dom', ...)` to spy on `useSearchParams` OR wrap in `<MemoryRouter>` — prefer MemoryRouter for realism.
    7. Build a `makeTask(overrides)` helper producing a ClickUpTask with defaults.
    8. Implement the four tests above using `describe('MeineAufgabenPage recommendations')`. Tests MUST currently FAIL (current MeineAufgabenPage does not render RecommendationsBlock).
    9. Use German strings from the current page: `"Keine offenen Aufgaben — alles erledigt!"`.
    10. Do NOT implement the feature — only the tests. This is Wave 0 RED.
  </action>
  <verify>
    <automated>npm run test -- src/shared/pages/__tests__/MeineAufgabenPage.test.tsx --run 2>&amp;1 | grep -E "(FAIL|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/shared/pages/__tests__/MeineAufgabenPage.test.tsx` exists
    - File contains 4 test cases (UI-01, UI-02 positive, UI-02 negative, UI-03)
    - `grep -c "it(\|test(" src/shared/pages/__tests__/MeineAufgabenPage.test.tsx` >= 4
    - `npm run test -- MeineAufgabenPage --run` exits with non-zero (tests fail — RED)
    - No changes to `src/shared/pages/MeineAufgabenPage.tsx` in this task
  </acceptance_criteria>
  <done>Four failing tests committed; MeineAufgabenPage source untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wave 0 — recommendation_reminder email copy test stub (RED)</name>
  <files>src/modules/tickets/__tests__/emailCopy-recommendation.test.ts</files>
  <read_first>
    - supabase/functions/_shared/emailCopy.ts (current EMAIL_COPY dict and EmailType union)
    - .planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-RESEARCH.md (lines 175–192 for expected German copy)
  </read_first>
  <behavior>
    Test 1 (EMAIL-01): `EMAIL_COPY.recommendation_reminder.de.subject(1)` returns `"Erinnerung: 1 offene Empfehlung wartet auf Ihre Entscheidung"`.
    Test 2 (EMAIL-01): `EMAIL_COPY.recommendation_reminder.de.subject(3)` returns `"Erinnerung: 3 offene Empfehlungen warten auf Ihre Entscheidung"`.
    Test 3 (EMAIL-01): `EMAIL_COPY.recommendation_reminder.de.cta === "Im Portal ansehen"`.
    Test 4 (EMAIL-01): `EMAIL_COPY.recommendation_reminder.de.title === "Offene Empfehlungen"`.
  </behavior>
  <action>
    Create `src/modules/tickets/__tests__/emailCopy-recommendation.test.ts`:
    1. Import `EMAIL_COPY` from `../../../../supabase/functions/_shared/emailCopy.ts` (relative path from test file).
       Note: Deno source imported into Vitest via ESM. If the file uses Deno-specific globals at top level, create a thin re-export file (`src/modules/tickets/__tests__/__fixtures__/emailCopy-import.ts`) that re-exports only `EMAIL_COPY` and `EmailType`. Verify import works; adjust tsconfig paths only if required.
    2. Write the 4 assertions listed in `<behavior>`.
    3. Tests MUST fail now because `recommendation_reminder` key does NOT yet exist in EMAIL_COPY — expected: `Cannot read properties of undefined`.
  </action>
  <verify>
    <automated>npm run test -- emailCopy-recommendation --run 2>&amp;1 | grep -E "(FAIL|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/modules/tickets/__tests__/emailCopy-recommendation.test.ts` exists
    - Contains 4 assertions referencing `recommendation_reminder`
    - `grep -c "recommendation_reminder" src/modules/tickets/__tests__/emailCopy-recommendation.test.ts` >= 4
    - `npm run test -- emailCopy-recommendation --run` exits non-zero (RED)
    - No changes to `supabase/functions/_shared/emailCopy.ts` in this task
  </acceptance_criteria>
  <done>Failing email copy test committed; emailCopy.ts untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave 0 — RecommendationsBlock Später session-snooze test stub (RED)</name>
  <files>src/modules/tickets/__tests__/RecommendationsBlock.test.tsx</files>
  <read_first>
    - src/modules/tickets/components/RecommendationsBlock.tsx (current props — does NOT yet accept onSnooze)
    - src/modules/tickets/components/RecommendationCard.tsx (current props — does NOT yet render Später button)
    - CLAUDE.md (Testing section — component render + QueryClientProvider wrapper)
  </read_first>
  <behavior>
    Test 1 (UI-02 Später): Render RecommendationsBlock with two recommendations and an `onSnooze` spy. Click the "Später" button on the first card → `onSnooze` is called once with the first recommendation's `clickup_id`. The card click handler (`onTaskClick`) MUST NOT fire (stopPropagation). This test exercises the RecommendationCard Später button directly, not the parent page state.
    Test 2 (UI-02 Später session-only): When `onSnooze` is undefined (current TicketsPage caller), the Später button MUST NOT be rendered — existing consumers remain unaffected. This ensures the prop is optional and backward-compatible.
  </behavior>
  <action>
    Create `src/modules/tickets/__tests__/RecommendationsBlock.test.tsx` as a RED-state test file:
    1. Use vitest + @testing-library/react + `@testing-library/user-event`.
    2. Build a `makeTask(overrides)` helper producing a ClickUpTask with recommendation tag + status 'to do'.
    3. Test 1: Render `<RecommendationsBlock recommendations={[task1, task2]} onTaskClick={onTaskClick} onSnooze={onSnooze} />` wrapped in `<MemoryRouter>`. Find the "Später" button within the first card (by text `"Später"`) and click it. Assert `onSnooze` called with `task1.clickup_id` and `onTaskClick` NOT called.
    4. Test 2: Render `<RecommendationsBlock recommendations={[task1]} onTaskClick={onTaskClick} />` (NO onSnooze). Assert `screen.queryByText("Später")` is null.
    5. Tests MUST currently FAIL: RecommendationsBlock/RecommendationCard do not yet accept `onSnooze`, and no "Später" button exists in the current source.
    6. Do NOT implement the feature — only the tests. This is Wave 0 RED. Plan 03 Task 1 makes these GREEN.
  </action>
  <verify>
    <automated>npm run test -- src/modules/tickets/__tests__/RecommendationsBlock.test.tsx --run 2>&amp;1 | grep -E "(FAIL|failed)"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/modules/tickets/__tests__/RecommendationsBlock.test.tsx` exists
    - Contains 2 test cases (Später click fires onSnooze + stopPropagation; optional prop backward-compat)
    - `grep -q "Später" src/modules/tickets/__tests__/RecommendationsBlock.test.tsx`
    - `grep -q "onSnooze" src/modules/tickets/__tests__/RecommendationsBlock.test.tsx`
    - `npm run test -- RecommendationsBlock --run` exits non-zero (RED)
    - No changes to `src/modules/tickets/components/RecommendationsBlock.tsx` or `RecommendationCard.tsx` in this task
  </acceptance_criteria>
  <done>Two failing tests committed; RecommendationsBlock and RecommendationCard source untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → migration | SQL executed against prod DB with service role privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Migration file | mitigate | Idempotent `ADD COLUMN IF NOT EXISTS` — re-running migration is a no-op; no data loss path |
| T-07-02 | Information Disclosure | New column | accept | Column stores a timestamp only, no PII; RLS on profiles already restricts row access to owning user + service role |
| T-07-03 | Denial of Service | `supabase db push` on wrong project | mitigate | Explicit two-step relink procedure (prod then staging then re-link prod) documented in Task 1 action |
</threat_model>

<verification>
- Migration file exists and contains expected column
- `supabase db push` applied to both prod and staging
- MeineAufgabenPage test file exists with 4 failing tests
- emailCopy test file exists with 4 failing tests
- RecommendationsBlock test file exists with 2 failing tests
- `npm run test` shows at least the three new test files as failing (RED)
- `src/shared/pages/MeineAufgabenPage.tsx`, `supabase/functions/_shared/emailCopy.ts`, `src/modules/tickets/components/RecommendationsBlock.tsx`, and `src/modules/tickets/components/RecommendationCard.tsx` unchanged
</verification>

<success_criteria>
- DB column present in both environments
- Wave 0 RED tests committed (3 test files total)
- Downstream plans (02, 03) unblocked
</success_criteria>

<output>
Create `.planning/phases/07-empfehlungen-in-reminders-and-meine-aufgaben-with-decision-w/07-01-SUMMARY.md`
</output>
