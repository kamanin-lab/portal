---
phase: 13-org-onboarding-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types/common.ts
  - src/shared/hooks/useAuth.ts
  - src/modules/tickets/hooks/useUnreadCounts.ts
  - src/modules/tickets/hooks/useSupportTaskChat.ts
  - src/shared/pages/InboxPage.tsx
  - src/shared/components/layout/Sidebar.tsx
  - src/shared/components/layout/MobileSidebarOverlay.tsx
  - src/shared/components/layout/BottomNav.tsx
autonomous: true
requirements:
  - ORG-CLEANUP-05

must_haves:
  truths:
    - "Frontend no longer reads profile.support_task_id from any component or hook"
    - "useUnreadCounts receives supportTaskId as a parameter (not via internal Supabase query)"
    - "InboxPage, Sidebar, MobileSidebarOverlay, BottomNav all filter notifications via org.support_task_id"
    - "Profile TypeScript interface has no clickup_list_ids, support_task_id, or clickup_chat_channel_id fields"
    - "npm run test passes after changes"
  artifacts:
    - path: "src/shared/types/common.ts"
      provides: "Cleaned Profile interface — no org-config fields"
    - path: "src/modules/tickets/hooks/useUnreadCounts.ts"
      provides: "fetchUnreadCounts accepts supportTaskId param instead of querying profiles"
    - path: "src/modules/tickets/hooks/useSupportTaskChat.ts"
      provides: "Reads support_task_id from useOrg() not useAuth()/profile"
  key_links:
    - from: "src/shared/components/layout/Sidebar.tsx"
      to: "useOrg().organization.support_task_id"
      via: "useOrg hook"
      pattern: "useOrg"
    - from: "src/modules/tickets/hooks/useUnreadCounts.ts"
      to: "supportTaskId parameter"
      via: "function param"
      pattern: "supportTaskId:"
---

<objective>
Remove all frontend reads of `profile.support_task_id`, `profile.clickup_list_ids`, and `profile.clickup_chat_channel_id`. These columns will be dropped from the `profiles` table in Wave 3; frontend code must stop reading them before the SQL migration runs.

Purpose: Unblocks the DB column drop in Plan 13-03. If this plan deploys first, the column drop is safe.
Output: 8 files updated. `useUnreadCounts` receives `supportTaskId` as a parameter from callers. Layout components and InboxPage read `support_task_id` from `useOrg()` instead of `useAuth()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/13-org-onboarding-cleanup/13-RESEARCH.md
@.planning/phases/13-org-onboarding-cleanup/13-PATTERNS.md

<interfaces>
<!-- Key contracts the executor needs. -->

From src/shared/hooks/useOrg.ts:
```typescript
export interface OrgContextValue {
  organization: Organization | null  // Organization has support_task_id: string | null
  orgRole: OrgRole
  isAdmin: boolean
  isMember: boolean
  isViewer: boolean
  isLoading: boolean
}

export function useOrg(): OrgContextValue
```

From src/shared/types/organization.ts:
```typescript
export interface Organization {
  id: string
  name: string
  slug: string
  clickup_list_ids: string[]
  nextcloud_client_root: string | null
  support_task_id: string | null
  clickup_chat_channel_id: string | null
  created_at: string
  updated_at: string
}
```

Current Profile interface (src/shared/types/common.ts) — fields to REMOVE:
- clickup_list_ids: string[] | null   ← DROP (now on Organization)
- support_task_id: string | null      ← DROP (now on Organization)
- clickup_chat_channel_id: string | null ← DROP (now on Organization)

Current useUnreadCounts signature (src/modules/tickets/hooks/useUnreadCounts.ts):
- fetchUnreadCounts(userId: string) — currently queries profiles.support_task_id internally
- useUnreadCounts(userId: string | undefined) — exported hook

Current useSupportTaskChat (src/modules/tickets/hooks/useSupportTaskChat.ts):
- Line 25: const supportTaskId = profile?.support_task_id ?? null
- Must change to: const { organization } = useOrg(); const supportTaskId = organization?.support_task_id ?? null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Clean Profile type and update useUnreadCounts to accept supportTaskId param</name>
  <files>src/shared/types/common.ts, src/modules/tickets/hooks/useUnreadCounts.ts, src/modules/tickets/__tests__/useUnreadCounts.test.ts</files>
  <behavior>
    - Profile interface no longer has clickup_list_ids, support_task_id, or clickup_chat_channel_id
    - fetchUnreadCounts(userId, supportTaskId) — supportTaskId passed in, no internal profile query
    - useUnreadCounts(userId, supportTaskId) — hook signature updated with second param
    - All existing behavior preserved (support count, task counts, needsReply) with the new param
    - When supportTaskId is null, supportCount stays 0 (same as current null-guard)
  </behavior>
  <action>
1. In `src/shared/types/common.ts`, remove these three fields from the `Profile` interface:
   - `clickup_list_ids: string[] | null`
   - `support_task_id: string | null`
   - `clickup_chat_channel_id: string | null`
   Keep all other fields (id, email, full_name, company_name, email_notifications, notification_preferences, avatar_url).

2. In `src/modules/tickets/hooks/useUnreadCounts.ts`:
   - Change `async function fetchUnreadCounts(userId: string): Promise<UnreadCounts>` to `async function fetchUnreadCounts(userId: string, supportTaskId: string | null): Promise<UnreadCounts>`
   - Remove the entire block at lines 15–24 (the Supabase `profiles.select('support_task_id')` query)
   - `supportTaskId` is now the passed-in parameter (no `profile?.support_task_id ?? null` derivation)
   - Change `useUnreadCounts(userId: string | undefined)` to `useUnreadCounts(userId: string | undefined, supportTaskId: string | null = null)`
   - Inside the hook, pass `supportTaskId` to `fetchUnreadCounts(userId!, supportTaskId)` in the `queryFn`
   - Update `queryKey` to `['unread-counts', userId, supportTaskId]` so cache is keyed per org config

3. Write/update tests in `src/modules/tickets/__tests__/useUnreadCounts.test.ts` verifying:
   - When supportTaskId is null, supportCount is 0 and no profiles query is made
   - When supportTaskId is provided, support comments matching that taskId are counted
  </action>
  <verify>
    <automated>npm run test -- --reporter=verbose src/modules/tickets/__tests__/useUnreadCounts.test.ts</automated>
  </verify>
  <done>Profile has no org-config fields. useUnreadCounts(userId, supportTaskId) signature works. Tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update useSupportTaskChat and all layout/page callers</name>
  <files>
    src/modules/tickets/hooks/useSupportTaskChat.ts,
    src/shared/pages/InboxPage.tsx,
    src/shared/components/layout/Sidebar.tsx,
    src/shared/components/layout/MobileSidebarOverlay.tsx,
    src/shared/components/layout/BottomNav.tsx,
    src/shared/hooks/useAuth.ts
  </files>
  <action>
1. `src/modules/tickets/hooks/useSupportTaskChat.ts`:
   - Replace `import { useAuth }` with `import { useOrg } from '@/shared/hooks/useOrg'` (keep useAuth for profile.full_name and user)
   - Keep `const { profile, user } = useAuth()` for the `userName` line
   - Change line 25: `const supportTaskId = profile?.support_task_id ?? null`
     to: `const { organization } = useOrg(); const supportTaskId = organization?.support_task_id ?? null`

2. `src/shared/pages/InboxPage.tsx`:
   - Add `import { useOrg } from '@/shared/hooks/useOrg'`
   - Add `const { organization } = useOrg()` inside InboxPage
   - Change the filter at line 27: `n => !profile?.support_task_id || n.task_id !== profile.support_task_id`
     to: `n => !organization?.support_task_id || n.task_id !== organization.support_task_id`
   - Remove `profile` from useAuth destructure if it is no longer used (check the rest of the component first)

3. `src/shared/components/layout/Sidebar.tsx`:
   - Add `import { useOrg } from '@/shared/hooks/useOrg'`
   - Add `const { organization } = useOrg()` next to the existing hooks
   - Change `useUnreadCounts(profile?.id)` to `useUnreadCounts(profile?.id, organization?.support_task_id ?? null)` — this now passes supportTaskId as second param
   - Change line 33: `n => !profile?.support_task_id || n.task_id !== profile.support_task_id`
     to: `n => !organization?.support_task_id || n.task_id !== organization.support_task_id`

4. `src/shared/components/layout/MobileSidebarOverlay.tsx`:
   - Add `import { useOrg } from '@/shared/hooks/useOrg'`
   - Add `const { organization } = useOrg()` inside the component
   - Replace `profile?.support_task_id` filter with `organization?.support_task_id`
   - Update `useUnreadCounts` call to pass `organization?.support_task_id ?? null` as second arg

5. `src/shared/components/layout/BottomNav.tsx`:
   - Same pattern as MobileSidebarOverlay: add useOrg import, replace profile.support_task_id with organization.support_task_id, update useUnreadCounts call

6. `src/shared/hooks/useAuth.ts`:
   - Find `STAGING_BYPASS_PROFILE` object (line ~36)
   - Remove `support_task_id: null`, `clickup_list_ids: null`, `clickup_chat_channel_id: null` fields from it (they no longer exist on Profile type)

   IMPORTANT: Do NOT remove `profile` from useAuth. `profile.full_name`, `profile.email`, `profile.email_notifications`, `profile.notification_preferences`, `profile.avatar_url`, and `profile.company_name` are still valid and used throughout the codebase.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    Build succeeds with zero TypeScript errors.
    No file references profile.support_task_id, profile.clickup_list_ids, or profile.clickup_chat_channel_id.
    Verified by: grep -r "profile\?\.support_task_id\|profile\.support_task_id" src/ returns no results.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OrgContext → component | organization may be null (user with no org_members row) — all callers guard with `?.` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Information Disclosure | Sidebar/InboxPage notification filter | accept | If org is null, filter passes all notifications (no support task ID to exclude) — safe because no data leakage, just extra inbox items |
| T-13-01-02 | Denial of Service | useUnreadCounts extra render if supportTaskId changes | accept | queryKey includes supportTaskId — React Query deduplicates; no thundering herd |
</threat_model>

<verification>
After both tasks complete:

```bash
# No profile org-config field references remain
grep -r "profile\?\.support_task_id\|profile\.support_task_id\|profile\.clickup_list_ids\|profile\.clickup_chat_channel_id" src/

# TypeScript build clean
npm run build

# Tests pass
npm run test
```

All three commands must return cleanly (grep: no output, build: no errors, test: all pass).
</verification>

<success_criteria>
- `src/shared/types/common.ts` Profile interface has exactly: id, email, full_name, company_name, email_notifications, notification_preferences, avatar_url
- `useUnreadCounts(userId, supportTaskId)` — second param exists, no internal profiles query
- `useSupportTaskChat` reads from `useOrg()` not `profile?.support_task_id`
- `Sidebar`, `MobileSidebarOverlay`, `BottomNav`, `InboxPage` all use `organization?.support_task_id`
- `npm run build` exits 0
- `npm run test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/13-org-onboarding-cleanup/13-01-SUMMARY.md`
</output>
