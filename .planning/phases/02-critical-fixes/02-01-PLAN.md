---
phase: 02-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/projects/types/project.ts
  - src/modules/projects/lib/transforms-project.ts
  - src/modules/projects/lib/helpers.ts
  - src/modules/projects/components/steps/StepOverviewTab.tsx
  - src/modules/projects/components/tasks/TasksPage.tsx
  - src/modules/projects/__tests__/transforms-project.test.ts
autonomous: true
requirements: [CRIT-01, CRIT-04]

must_haves:
  truths:
    - "The dead TasksPage component and its dead-code pipeline (project.tasks) are fully removed"
    - "StepOverviewTab never renders an expandable section with an empty body"
    - "StepOverviewTab no longer references getTasksForStep or linked tasks"
    - "Existing transform tests still pass after removing the tasks field"
  artifacts:
    - path: "src/modules/projects/types/project.ts"
      provides: "Project type without tasks field and without ProjectTask/TaskStatus types"
      contains: "interface Project"
    - path: "src/modules/projects/lib/transforms-project.ts"
      provides: "Transform function without tasks: [] output"
    - path: "src/modules/projects/components/steps/StepOverviewTab.tsx"
      provides: "Step overview with empty-body guards on ExpandableSection"
    - path: "src/modules/projects/__tests__/transforms-project.test.ts"
      provides: "Updated tests without project.tasks assertions"
  key_links:
    - from: "StepOverviewTab.tsx"
      to: "ExpandableSection"
      via: "empty body guard"
      pattern: "body\\.trim\\(\\).*===.*''"
---

<objective>
Remove the dead TasksPage data pipeline and fix empty enrichment sections in StepOverviewTab.

Purpose: CRIT-01 removes unreachable dead code (TasksPage is not routed, project.tasks is always []) to eliminate confusion and reduce type surface. CRIT-04 prevents blank expandable sections from rendering when AI enrichment body is empty.

Output: Cleaned Project type, cleaned transform, guarded ExpandableSection, removed dead component + helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/audits/projects-module-audit.md (Section 3.1.1, 3.1.4, Phase A)
@.planning/phases/02-critical-fixes/02-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/modules/projects/types/project.ts (CURRENT — to be modified):
```typescript
export type TaskStatus = 'needs-attention' | 'in-progress';

export interface ProjectTask {
  id: string;
  title: string;
  status: TaskStatus;
  stepId: string;
}

export interface Step {
  // ...
  messages: Message[];       // always [] — but NOT removed in this plan (Phase 5)
  // ...
}

export interface Project {
  // ...
  tasks: ProjectTask[];      // always [] — REMOVE in this plan
  // ...
}
```

From src/modules/projects/lib/helpers.ts (CURRENT — to be modified):
```typescript
export function getTasksForStep(stepId: string, project: Project): ProjectTask[] {
  return project.tasks.filter(t => t.stepId === stepId);
}

export function taskStatusLabel(status: string): string {
  // ... REMOVE — only consumer is linked tasks in StepOverviewTab
}
```

From src/modules/projects/lib/transforms-project.ts line 229:
```typescript
tasks: [] as ProjectTask[],   // REMOVE this line
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead TasksPage pipeline — types, transform, helpers, component (CRIT-01)</name>
  <files>
    src/modules/projects/types/project.ts,
    src/modules/projects/lib/transforms-project.ts,
    src/modules/projects/lib/helpers.ts,
    src/modules/projects/components/tasks/TasksPage.tsx,
    src/modules/projects/__tests__/transforms-project.test.ts
  </files>
  <read_first>
    src/modules/projects/types/project.ts,
    src/modules/projects/lib/transforms-project.ts,
    src/modules/projects/lib/helpers.ts,
    src/modules/projects/components/tasks/TasksPage.tsx,
    src/modules/projects/__tests__/transforms-project.test.ts,
    src/app/routes.tsx
  </read_first>
  <action>
    TasksPage is confirmed NOT routed in routes.tsx. Per CRIT-01 decision: remove the dead pipeline entirely.

    **Step 1 — Verify no other consumers exist.** Search the codebase for:
    - imports of `TasksPage` (should be zero outside its own file)
    - imports of `ProjectTask` type (only used in TasksPage, helpers.ts, transforms-project.ts, project.ts)
    - imports of `TaskStatus` type (only used in project.ts for ProjectTask)
    - usages of `getTasksForStep` (only StepOverviewTab — handled in Task 2 of this plan)
    - usages of `taskStatusLabel` (only StepOverviewTab — handled in Task 2 of this plan)

    **Step 2 — Remove from types/project.ts:**
    - Delete the `TaskStatus` type alias (`type TaskStatus = 'needs-attention' | 'in-progress'`)
    - Delete the `ProjectTask` interface (4 lines: id, title, status, stepId)
    - Remove `tasks: ProjectTask[]` from the `Project` interface
    - Leave the `Step.messages: Message[]` field in place (NOT part of this fix — deferred)

    **Step 3 — Remove from transforms-project.ts:**
    - Remove the `import` of `ProjectTask` from the type imports at line 1
    - Remove line 229: `tasks: [] as ProjectTask[],` from the return object in `transformToProject()`

    **Step 4 — Remove from helpers.ts:**
    - Remove the `getTasksForStep` function entirely (lines 83-85)
    - Remove the `taskStatusLabel` function entirely (lines 104-111)
    - Remove `ProjectTask` from the import statement at line 1
    - Keep all other helpers intact (getNextCheckpoint, generateNarrative, statusLabel, etc.)

    **Step 5 — Delete TasksPage component:**
    - Delete the file `src/modules/projects/components/tasks/TasksPage.tsx` entirely

    **Step 6 — Update tests:**
    - In transforms-project.test.ts, remove any assertions that reference `project.tasks`
    - The test "computes tasks summary" is fine — tasksSummary uses allSteps, not project.tasks
    - Verify all remaining tests still compile and pass
  </action>
  <verify>
    <automated>cd "G:/01_OPUS/Projects/PORTAL" && npx vitest run src/modules/projects/__tests__/transforms-project.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "ProjectTask" src/modules/projects/ returns zero matches (type fully removed)
    - grep -r "TaskStatus" src/modules/projects/types/project.ts returns zero matches
    - grep -r "getTasksForStep" src/modules/projects/ returns zero matches
    - grep -r "taskStatusLabel" src/modules/projects/ returns zero matches
    - File src/modules/projects/components/tasks/TasksPage.tsx does NOT exist
    - grep "tasks:" src/modules/projects/lib/transforms-project.ts returns zero matches (no tasks field in return)
    - npm run build succeeds with zero TypeScript errors
    - vitest run transforms-project.test.ts passes all tests
  </acceptance_criteria>
  <done>
    The dead TasksPage data pipeline is fully removed: no ProjectTask type, no tasks field on Project, no getTasksForStep helper, no taskStatusLabel helper, no TasksPage component file. Build and tests pass clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Guard empty enrichment sections in StepOverviewTab (CRIT-04)</name>
  <files>
    src/modules/projects/components/steps/StepOverviewTab.tsx
  </files>
  <read_first>
    src/modules/projects/components/steps/StepOverviewTab.tsx,
    src/modules/projects/types/project.ts
  </read_first>
  <action>
    StepOverviewTab renders ExpandableSection for whyItMatters and whatBecomesFixed even when those strings are empty, producing blank expand areas. Fix both the ExpandableSection component and the StepOverviewTab consumer.

    **Step 1 — Clean up StepOverviewTab after Task 1 removals:**
    - Remove the import of `getTasksForStep` and `taskStatusLabel` from `../../lib/helpers` (these were deleted in Task 1)
    - Remove the `import type { Project, Step } from '../../types/project'` — change to `import type { Step } from '../../types/project'` (Project is no longer needed since we remove linkedTasks)
    - Remove `project` from the component props interface (StepOverviewTabProps should only have `step: Step`)
    - Remove `const linkedTasks = getTasksForStep(step.id, project);`
    - Remove the entire `{linkedTasks.length > 0 && (...)}` JSX block (lines 52-86)
    - Remove `linkedTasks: false` from the expandedSections state
    - Update the parent caller if needed (grep for `<StepOverviewTab` to find where project prop is passed — remove the project prop)

    **Step 2 — Add empty-body guard to ExpandableSection:**
    - In the ExpandableSection component (same file, bottom), add an early return at the top of the function:
      ```typescript
      if (!body || body.trim() === '') return null;
      ```
    - This ensures that whyItMatters and whatBecomesFixed sections with empty strings never render

    **Step 3 — Handle the "Was ist das?" description section:**
    - The description ExpandableSection (`title="Was ist das?"`) uses `defaultOpen` and `toggleable={false}`. If description is also empty, it should ALSO be hidden. The guard from Step 2 handles this automatically.

    **Step 4 — Find and update the parent that passes `project` prop to StepOverviewTab:**
    - Search for `<StepOverviewTab` in the codebase
    - Remove the `project={...}` prop from the call site
    - This is likely in StepDetail.tsx or similar component
  </action>
  <verify>
    <automated>cd "G:/01_OPUS/Projects/PORTAL" && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "getTasksForStep" src/modules/projects/components/steps/StepOverviewTab.tsx returns zero matches
    - grep "taskStatusLabel" src/modules/projects/components/steps/StepOverviewTab.tsx returns zero matches
    - grep "linkedTasks" src/modules/projects/components/steps/StepOverviewTab.tsx returns zero matches
    - grep "project" src/modules/projects/components/steps/StepOverviewTab.tsx returns zero matches (project prop fully removed)
    - grep "body.trim()" src/modules/projects/components/steps/StepOverviewTab.tsx returns a match (the guard is present)
    - npm run build succeeds with zero errors
  </acceptance_criteria>
  <done>
    StepOverviewTab no longer renders blank expandable sections — empty whyItMatters, whatBecomesFixed, or description strings produce no DOM output. The dead linked-tasks section and its Project dependency are removed. Build passes clean.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` passes with zero errors
2. `npx vitest run src/modules/projects/` — all project module tests pass
3. Grep confirms no references to removed types: `grep -r "ProjectTask\|TaskStatus\|getTasksForStep\|taskStatusLabel" src/modules/projects/` returns nothing
4. Grep confirms empty-body guard exists: `grep "body.trim()" src/modules/projects/components/steps/StepOverviewTab.tsx` returns a match
</verification>

<success_criteria>
- The ProjectTask type, TaskStatus type, getTasksForStep function, taskStatusLabel function, and TasksPage component are fully removed from the codebase
- ExpandableSection in StepOverviewTab returns null when body is empty or whitespace-only
- StepOverviewTab has no dependency on Project type or linked tasks logic
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/02-critical-fixes/02-01-SUMMARY.md`
</output>
