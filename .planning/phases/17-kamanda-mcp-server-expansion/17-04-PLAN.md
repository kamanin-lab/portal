---
phase: 17-kamanda-mcp-server-expansion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - mcp-poc:.env.example
autonomous: true
requirements: [MCPS-07]
tags: [mcp-poc, config, env-vars, runbook]

must_haves:
  truths:
    - "`mcp-poc/.env.example` documents 3 new env vars: `WOOCOMMERCE_WP_USER`, `WOOCOMMERCE_WP_APP_PASS`, `KMN_BRIDGE_URL`"
    - "Each var has an inline comment explaining its purpose and how to obtain the value"
    - "Comments explicitly note the distinction from the portal's `WP_MCP_USER` / `WP_MCP_APP_PASS` (no credential coupling per MCPS-07 / D-19)"
    - "Pre-existing env vars (WOOCOMMERCE_*, PORT) remain unchanged — no reordering, no deletion"
    - "The file contains no literal secrets — all new vars are empty `NAME=` placeholders"
  artifacts:
    - path: "mcp-poc:.env.example"
      provides: "Documented env var list for local dev + Vercel deployment"
      contains: "WOOCOMMERCE_WP_USER="
      contains: "WOOCOMMERCE_WP_APP_PASS="
      contains: "KMN_BRIDGE_URL="
  key_links:
    - from: "mcp-poc:.env.example"
      to: "mcp-poc:src/connectors/kmn-bridge.ts"
      via: "env var names consumed by loadKmnBridgeCredentialsFromEnv()"
      pattern: "KMN_BRIDGE_URL\\|WOOCOMMERCE_WP_USER\\|WOOCOMMERCE_WP_APP_PASS"
---

<objective>
Append the 3 new env vars consumed by `kmn-bridge.ts` (Plan 01) to `mcp-poc/.env.example` with inline-comment documentation following the existing file style.

Purpose: Satisfies MCPS-07. Makes the required env vars discoverable for both local developers (who copy `.env.example` → `.env.local`) and for the Vercel-deployment runbook (Yuri pastes them into the project's environment-variable dashboard before deploying). Per D-19 these names are deliberately distinct from the portal-side `WP_MCP_USER` / `WP_MCP_APP_PASS` used by `wp-audit.ts` — no credential coupling.

Output: Modified `G:/01_OPUS/Projects/mcp-poc/.env.example`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@G:/01_OPUS/Projects/PORTAL/.planning/phases/17-kamanda-mcp-server-expansion/17-CONTEXT.md
@G:/01_OPUS/Projects/PORTAL/.planning/phases/17-kamanda-mcp-server-expansion/17-PATTERNS.md
@G:/01_OPUS/Projects/mcp-poc/.env.example

<current_file_state>
<!-- The 20 existing lines — must be preserved byte-for-byte before the new block is appended. -->

```
# WooCommerce store base URL, e.g. https://mbm.example.com
WOOCOMMERCE_STORE_URL=

# REST API keys from WooCommerce → Settings → Advanced → REST API
WOOCOMMERCE_CONSUMER_KEY=
WOOCOMMERCE_CONSUMER_SECRET=

# Admin URL if different from {STORE_URL}/wp-admin.
# Leave empty to use the standard wp-admin path.
WOOCOMMERCE_ADMIN_URL=

# Surface on-hold orders in the payment-attention tool.
# Default: off. Many shops use `on-hold` for "waiting on bank transfer" with
# no downstream sync back to Woo, so the state is noise not signal.
# Enable ("true") only when on-hold genuinely means "needs manual review".
WOOCOMMERCE_INCLUDE_ON_HOLD=false

# Local dev only
PORT=3000
```
</current_file_state>

<placement>
<!-- Per PATTERNS §9 — insert the new block between `WOOCOMMERCE_INCLUDE_ON_HOLD=false` and `# Local dev only`. This groups WordPress-facing vars together and keeps `PORT=3000` at the bottom as the "local-only" divider. -->
</placement>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append 3 new env vars with inline-comment docs to `.env.example`</name>
  <files>G:/01_OPUS/Projects/mcp-poc/.env.example</files>

  <read_first>
    - G:/01_OPUS/Projects/mcp-poc/.env.example (full current contents — preserve byte-for-byte before the append)
    - G:/01_OPUS/Projects/PORTAL/.planning/phases/17-kamanda-mcp-server-expansion/17-CONTEXT.md (D-18 for exact comment phrasings, D-19 for the no-coupling assertion)
    - G:/01_OPUS/Projects/PORTAL/.planning/phases/17-kamanda-mcp-server-expansion/17-PATTERNS.md §9 (exact block to append)
  </read_first>

  <behavior>
    - All 20 existing lines preserved verbatim (no reordering, no reformatting, no trailing-whitespace trims on existing lines)
    - Exactly 3 new `NAME=` lines added with empty values
    - Each new var preceded by 2-3 inline comment lines in the same style as the existing file (`# `-prefixed sentences, wrapping at ~80 chars)
    - The comment on `WOOCOMMERCE_WP_USER` / `WOOCOMMERCE_WP_APP_PASS` explicitly mentions the distinction from `WP_MCP_USER` / `WP_MCP_APP_PASS` (MCPS-07 assertion)
    - The comment on `KMN_BRIDGE_URL` mentions the DDEV-only scope for v3.0
    - New block inserted between the `WOOCOMMERCE_INCLUDE_ON_HOLD=false` line and the `# Local dev only` comment (per PATTERNS §9)
  </behavior>

  <action>
Replace the contents of `G:/01_OPUS/Projects/mcp-poc/.env.example` with the following exact text. The existing 20 lines are preserved verbatim; the new 10-line block is inserted between line 16 (`WOOCOMMERCE_INCLUDE_ON_HOLD=false`) and the blank line before `# Local dev only`.

```
# WooCommerce store base URL, e.g. https://mbm.example.com
WOOCOMMERCE_STORE_URL=

# REST API keys from WooCommerce → Settings → Advanced → REST API
WOOCOMMERCE_CONSUMER_KEY=
WOOCOMMERCE_CONSUMER_SECRET=

# Admin URL if different from {STORE_URL}/wp-admin.
# Leave empty to use the standard wp-admin path.
WOOCOMMERCE_ADMIN_URL=

# Surface on-hold orders in the payment-attention tool.
# Default: off. Many shops use `on-hold` for "waiting on bank transfer" with
# no downstream sync back to Woo, so the state is noise not signal.
# Enable ("true") only when on-hold genuinely means "needs manual review".
WOOCOMMERCE_INCLUDE_ON_HOLD=false

# WordPress Application Password for the kmn-revenue-abilities MCP bridge.
# Generate in wp-admin → Users → Application Passwords.
# DISTINCT from WP_MCP_USER / WP_MCP_APP_PASS used by the portal's
# wp-audit Edge Function — no credential coupling (see MCPS-07).
WOOCOMMERCE_WP_USER=
WOOCOMMERCE_WP_APP_PASS=

# Full URL to the kmn-revenue MCP bridge endpoint, e.g.
#   https://summerfield.ddev.site/wp-json/mcp/kmn-revenue
# v3.0 targets Summerfield DDEV only; production bridge URL is
# deferred to the future MBM rollout milestone.
KMN_BRIDGE_URL=

# Local dev only
PORT=3000
```

Important details:
- The new block follows the existing comment style: every comment line begins with `# ` (hash + single space), sentences end with a period, the block is separated from its neighbours by blank lines.
- `WOOCOMMERCE_WP_USER=` and `WOOCOMMERCE_WP_APP_PASS=` share the same comment block (4 comment lines above both var names) because they describe a single credential pair — matches how `WOOCOMMERCE_CONSUMER_KEY` + `_SECRET` share one comment block at lines 4-6.
- `KMN_BRIDGE_URL=` has its own 4-line comment block (example URL on the second comment line indented with 2 spaces for readability, matching how existing files document example values).
- No trailing whitespace on any line. Final file ends with a single newline after `PORT=3000`.
- All new `NAME=` lines have empty values (no secrets committed — this is `.env.example`).

Do NOT:
- Reorder existing vars
- Change existing comment wording (even trivially)
- Remove the blank lines between blocks
- Add the actual DDEV Application Password as a default value (.env.local is where secrets live, not .env.example)
- Rename `.env.example` or create an additional env file
  </action>

  <verify>
    <automated>test -f G:/01_OPUS/Projects/mcp-poc/.env.example &amp;&amp; echo EXISTS || (echo MISSING; exit 1)</automated>
    <automated>grep -c "^WOOCOMMERCE_WP_USER=$" G:/01_OPUS/Projects/mcp-poc/.env.example</automated>
    <automated>grep -c "^WOOCOMMERCE_WP_APP_PASS=$" G:/01_OPUS/Projects/mcp-poc/.env.example</automated>
    <automated>grep -c "^KMN_BRIDGE_URL=$" G:/01_OPUS/Projects/mcp-poc/.env.example</automated>
    <automated>grep -c "WP_MCP_USER\|WP_MCP_APP_PASS" G:/01_OPUS/Projects/mcp-poc/.env.example</automated>
    <automated>grep -c "^WOOCOMMERCE_STORE_URL=$\|^WOOCOMMERCE_CONSUMER_KEY=$\|^WOOCOMMERCE_CONSUMER_SECRET=$\|^WOOCOMMERCE_ADMIN_URL=$\|^WOOCOMMERCE_INCLUDE_ON_HOLD=false$\|^PORT=3000$" G:/01_OPUS/Projects/mcp-poc/.env.example</automated>
  </verify>

  <acceptance_criteria>
    - File `G:/01_OPUS/Projects/mcp-poc/.env.example` exists
    - File contains `^WOOCOMMERCE_WP_USER=$` (grep count == 1 — empty placeholder, not `WOOCOMMERCE_WP_USER=some-value`)
    - File contains `^WOOCOMMERCE_WP_APP_PASS=$` (grep count == 1 — empty placeholder)
    - File contains `^KMN_BRIDGE_URL=$` (grep count == 1 — empty placeholder)
    - File references `WP_MCP_USER` or `WP_MCP_APP_PASS` in the comment mentioning the distinction (grep count >= 1 — confirms D-19 assertion is documented)
    - All 6 pre-existing vars preserved with exact same values: `^WOOCOMMERCE_STORE_URL=$`, `^WOOCOMMERCE_CONSUMER_KEY=$`, `^WOOCOMMERCE_CONSUMER_SECRET=$`, `^WOOCOMMERCE_ADMIN_URL=$`, `^WOOCOMMERCE_INCLUDE_ON_HOLD=false$`, `^PORT=3000$` (grep count == 6)
    - No actual secrets committed — grep for common Application Password patterns (`[a-zA-Z0-9]{24}\s*$`) returns 0 matches (app passwords are 24 alnum chars)
    - Example URL `https://summerfield.ddev.site/wp-json/mcp/kmn-revenue` appears in a comment line (not as a default value — grep the URL, confirm the matching line starts with `#`)
  </acceptance_criteria>

  <done>
    `.env.example` updated with 3 new vars (`WOOCOMMERCE_WP_USER`, `WOOCOMMERCE_WP_APP_PASS`, `KMN_BRIDGE_URL`), each empty, each with inline comments explaining purpose + the MCPS-07 no-coupling distinction. Existing vars unchanged.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No runtime trust boundaries introduced — `.env.example` is build-time documentation only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-14 | Information disclosure | Committing real Application Password to `.env.example` | mitigate | Acceptance criteria grep for any 24-alphanumeric-char suffix at end-of-line (common WP App Password shape) — matches == 0. All new var values are empty (`NAME=`). |
| T-17-15 | Spoofing | Credential coupling with portal's `WP_MCP_*` | mitigate | Comment block on `WOOCOMMERCE_WP_USER` explicitly names `WP_MCP_USER` / `WP_MCP_APP_PASS` and declares "no credential coupling". Future operators reading the file see the distinction immediately. |
| T-17-16 | Information disclosure | `.env.example` gets read by third parties | accept | File is already part of the public repo; contains no secrets by definition; documented DDEV example URL is staging-only (`*.ddev.site` resolves only on developer machines). |
</threat_model>

<verification>
- `.env.example` has 3 new empty-value vars with correct names
- Existing 6 vars unchanged
- Comment on `WOOCOMMERCE_WP_USER` / `WOOCOMMERCE_WP_APP_PASS` mentions the `WP_MCP_*` distinction
- No real secrets in the file (24-char alnum pattern on any `NAME=<value>` line matches 0)
</verification>

<success_criteria>
- 3 new env vars documented with the exact names `WOOCOMMERCE_WP_USER`, `WOOCOMMERCE_WP_APP_PASS`, `KMN_BRIDGE_URL`
- Each has inline comment docs (style-matching the existing file)
- No-credential-coupling assertion (MCPS-07) preserved in the comment
- All pre-existing vars unchanged
</success_criteria>

<output>
After completion, create `G:/01_OPUS/Projects/PORTAL/.planning/phases/17-kamanda-mcp-server-expansion/17-04-SUMMARY.md`
</output>
