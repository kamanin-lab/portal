---
phase: 04-phasetimeline-redesign
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/modules/projects/__tests__/PhaseTimeline.test.tsx
autonomous: true
requirements: [TIMELINE-01, TIMELINE-02, TIMELINE-03, TIMELINE-04, TIMELINE-05]

must_haves:
  truths:
    - "A test file exists with failing stubs before any PhaseTimeline implementation begins"
    - "Each TIMELINE requirement has at least one test stub that will fail until implementation is complete"
  artifacts:
    - path: "src/modules/projects/__tests__/PhaseTimeline.test.tsx"
      provides: "Failing test stubs for all 5 TIMELINE requirements"
      contains: "describe.*PhaseTimeline"
  key_links:
    - from: "PhaseTimeline.test.tsx"
      to: "PhaseTimeline.tsx"
      via: "imports and renders PhaseTimeline component"
      pattern: "import.*PhaseTimeline"
---

<objective>
Create the Wave 0 test scaffold for PhaseTimeline with failing stubs covering TIMELINE-01 through TIMELINE-05. This file is the behavioral verification foundation that Plan 01 and Plan 02 build against.

Purpose: Satisfies Nyquist sampling requirement -- no implementation task runs without an automated behavioral test available. Stubs fail initially (RED), and pass as Plans 01 and 02 implement the components.
Output: `PhaseTimeline.test.tsx` with describe blocks and failing test stubs for all 5 requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-phasetimeline-redesign/04-RESEARCH.md

@src/modules/projects/__tests__/DynamicHero.test.tsx
@src/modules/projects/types/project.ts
@src/modules/projects/lib/helpers.ts
@src/modules/projects/components/overview/PhaseTimeline.tsx
@src/modules/projects/components/overview/PhaseNode.tsx

<interfaces>
<!-- From existing codebase -->

From src/modules/projects/types/project.ts:
```typescript
export type ChapterStatus = 'completed' | 'current' | 'upcoming';

export interface Chapter {
  id: string;
  title: string;
  order: number;
  narrative: string;
  nextNarrative: string;
  clickupCfOptionId: string | null;
  steps: Step[];
}

export interface Step {
  id: string;
  title: string;
  status: StepStatus;
  enrichment: StepEnrichment | null;
  tasks: StepTask[];
}
```

From src/modules/projects/lib/helpers.ts:
```typescript
export function getChapterStatus(chapter: Chapter, project: Project): ChapterStatus;
export function getChapterProgress(chapter: Chapter): string; // "N/M"
```

From src/shared/hooks/useBreakpoint.ts:
```typescript
export function useBreakpoint(): { isMobile: boolean; isTablet: boolean; isDesktop: boolean; width: number };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PhaseTimeline.test.tsx with failing stubs for TIMELINE-01 through TIMELINE-05</name>
  <files>
    src/modules/projects/__tests__/PhaseTimeline.test.tsx
  </files>
  <read_first>
    src/modules/projects/__tests__/DynamicHero.test.tsx
    src/modules/projects/types/project.ts
    src/modules/projects/lib/helpers.ts
    src/modules/projects/components/overview/PhaseTimeline.tsx
    src/modules/projects/components/overview/PhaseNode.tsx
  </read_first>
  <action>
Create a test file following the DynamicHero.test.tsx pattern (same mock approach, `makeProject` factory).

**Mocks required:**
```typescript
// Mock useBreakpoint -- default to desktop
const mockUseBreakpoint = vi.fn(() => ({ isMobile: false, isTablet: false, isDesktop: true, width: 1200 }));
vi.mock('@/shared/hooks/useBreakpoint', () => ({
  useBreakpoint: () => mockUseBreakpoint(),
}));

// Mock motion/react -- render children without animation in jsdom
vi.mock('motion/react', () => ({
  motion: {
    div: ({ children, ...props }: any) => <div {...filterMotionProps(props)}>{children}</div>,
    span: ({ children, ...props }: any) => <span {...filterMotionProps(props)}>{children}</span>,
  },
  AnimatePresence: ({ children }: any) => <>{children}</>,
}));

// Mock @radix-ui/react-tooltip -- render children directly
vi.mock('@radix-ui/react-tooltip', () => ({
  Provider: ({ children }: any) => <>{children}</>,
  Root: ({ children }: any) => <>{children}</>,
  Trigger: ({ children }: any) => <>{children}</>,
  Content: ({ children }: any) => <div data-testid="tooltip-content">{children}</div>,
  Portal: ({ children }: any) => <>{children}</>,
}));
```

Create a `filterMotionProps` helper that strips Motion-specific props (`animate`, `initial`, `exit`, `variants`, `custom`, `transition`, `layout`, `whileHover`, `whileTap`) so they don't warn when spread onto DOM elements.

**makeProject factory** -- reuse pattern from DynamicHero.test.tsx with 4 chapters:
- Chapter 1 ("Konzept", order 1): 2 steps both status 'committed' (completed chapter)
- Chapter 2 ("Struktur", order 2): 1 step 'awaiting_input', 1 step 'upcoming_locked' (current chapter)
- Chapter 3 ("Design", order 3): 1 step 'upcoming_locked' (upcoming chapter)
- Chapter 4 ("Entwicklung", order 4): 1 step 'upcoming_locked' (upcoming chapter)

Also create `makeChapter` and `makeStep` helper factories.

**Test groups (all stubs should use `test.todo` or `test.skip` so they are recognized as pending, not passing):**

1. **describe('TIMELINE-01: Node size and phase colors')**
   - `test.todo('renders all 4 chapter titles')`
   - `test.todo('completed chapter shows Abgeschlossen label')`
   - `test.todo('current chapter shows Aktuell label')`

2. **describe('TIMELINE-02: Connector partial fill')**
   - `test.todo('renders connectors between chapters on desktop')`

3. **describe('TIMELINE-03: Motion animations')**
   - `test.todo('state labels are rendered (AnimatePresence mock renders children)')` -- Note: this is an automated proxy for visual animation verification per TIMELINE-03

4. **describe('TIMELINE-04: Mobile single-phase view')**
   - `test.todo('shows only current chapter on mobile')`
   - `test.todo('shows page indicator on mobile')`
   - `test.todo('prev button has German aria-label')`
   - `test.todo('clicking next shows next chapter')`

5. **describe('TIMELINE-05: Tooltip content')**
   - `test.todo('renders tooltip content with chapter.narrative')`

The file should import PhaseTimeline from its current location. The mocks, factories, and describe/test structure must all be present. The `test.todo` stubs ensure the file is valid and runnable (vitest reports them as "todo" not "fail"), while clearly communicating what needs to be implemented.
  </action>
  <verify>
    <automated>cd "G:/01_OPUS/Projects/PORTAL" && npx tsc --noEmit --pretty 2>&1 | head -20 && npm run test -- --reporter=verbose src/modules/projects/__tests__/PhaseTimeline.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>PhaseTimeline.test.tsx exists with mocks, factories, and test.todo stubs covering all 5 TIMELINE requirements. File compiles. Vitest recognizes the file and reports todo stubs. No tests pass yet (RED state).</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0 -- test file compiles
2. `npm run test -- --reporter=verbose src/modules/projects/__tests__/PhaseTimeline.test.tsx` -- file is recognized by vitest, reports todo stubs
3. Test file contains describe blocks for TIMELINE-01 through TIMELINE-05
</verification>

<success_criteria>
- PhaseTimeline.test.tsx exists at src/modules/projects/__tests__/
- Contains describe blocks for all 5 TIMELINE requirements
- Contains mock setup for useBreakpoint, motion/react, and @radix-ui/react-tooltip
- Contains makeProject, makeChapter, makeStep factories
- All tests are todo stubs (RED state -- none pass until Plans 01 and 02 implement)
- File compiles and vitest recognizes it
</success_criteria>

<output>
After completion, create `.planning/phases/04-phasetimeline-redesign/04-00-SUMMARY.md`
</output>
