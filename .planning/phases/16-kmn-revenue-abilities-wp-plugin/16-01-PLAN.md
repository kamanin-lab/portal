---
phase: 16-kmn-revenue-abilities-wp-plugin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wordpress-plugins/kmn-revenue-abilities/kmn-revenue-abilities.php
  - wordpress-plugins/kmn-revenue-abilities/bootstrap/register-mcp-server.php
  - wordpress-plugins/kmn-revenue-abilities/includes/sql-helpers.php
  - wordpress-plugins/kmn-revenue-abilities/includes/cache.php
  - wordpress-plugins/kmn-revenue-abilities/includes/response.php
  - wordpress-plugins/kmn-revenue-abilities/includes/rate-limit.php
  - wordpress-plugins/kmn-revenue-abilities/includes/cli-command.php
  - wordpress-plugins/kmn-revenue-abilities/readme.md
autonomous: true
requirements:
  - MCPAPP-WP-01
  - MCPAPP-WP-02
  - MCPAPP-WP-03
  - ABIL-SCAF-01
  - ABIL-SCAF-02
  - ABIL-SCAF-03
  - ABIL-SCAF-04
  - ABIL-SCAF-05

must_haves:
  truths:
    - "Plugin activates on DDEV (Summerfield) under WP 6.9.4 + WC 8.x without fatal"
    - "MCP endpoint at https://summerfield.ddev.site/wp-json/mcp/kmn-revenue responds 200 to tools/list JSON-RPC request, returning an empty tools array (no abilities registered in this plan)"
    - "Running `ddev wp kmn ability test <id>` with no id listed prints 'no abilities registered yet' and exits 0 (scaffolded, ready for Plan 16-02)"
    - "Shared helpers load without errors: kmn_revenue_response(), kmn_revenue_cache_key(), kmn_revenue_cached(), kmn_revenue_get_utc_offset()"
    - "Cache invalidation hook on woocommerce_order_status_changed is registered"
    - "readme.md documents plugin version, WP 6.9+ / PHP 8.1+ requirements, DDEV install steps, Application Password rotation runbook, WP-CLI ability-test command"
  artifacts:
    - path: "wordpress-plugins/kmn-revenue-abilities/kmn-revenue-abilities.php"
      provides: "Main plugin file with WP 6.9+ guard, WC dependency check, composer autoload, lazy ability loader (eager on WP_CLI, deferred to rest_api_init priority 0 otherwise)"
      min_lines: 80
    - path: "wordpress-plugins/kmn-revenue-abilities/bootstrap/register-mcp-server.php"
      provides: "MCP server registration via $adapter->create_server() with corrected v0.5.0 signature"
      contains: "create_server"
    - path: "wordpress-plugins/kmn-revenue-abilities/includes/sql-helpers.php"
      provides: "kmn_revenue_get_utc_offset(), kmn_revenue_resolve_tz_offset(), kmn_revenue_date_range_*(), kmn_revenue_status_whitelist(), kmn_revenue_prepare_in_placeholders()"
    - path: "wordpress-plugins/kmn-revenue-abilities/includes/cache.php"
      provides: "kmn_revenue_cache_key(), kmn_revenue_cached(), kmn_revenue_invalidate_all(); TTL constants KMN_REVENUE_TTL_RUN_RATE=300, KMN_REVENUE_TTL_DEFAULT=900"
    - path: "wordpress-plugins/kmn-revenue-abilities/includes/response.php"
      provides: "kmn_revenue_response() envelope helper forked from maxi_ai_response() minus operator-notes metadata"
    - path: "wordpress-plugins/kmn-revenue-abilities/includes/rate-limit.php"
      provides: "kmn_revenue_rate_limit_check() stub — 20 lines, transient-backed counter, NOT activated in v0.5.0 (deferred to v3.1)"
    - path: "wordpress-plugins/kmn-revenue-abilities/includes/cli-command.php"
      provides: "WP-CLI `wp kmn ability test <id>` for direct execute_callback invocation (no HTTP)"
    - path: "wordpress-plugins/kmn-revenue-abilities/readme.md"
      provides: "Setup, ability-test CLI, App Password rotation runbook"
  key_links:
    - from: "kmn-revenue-abilities.php"
      to: "includes/*.php + bootstrap/register-mcp-server.php"
      via: "require_once loader (eager on WP_CLI, lazy on rest_api_init priority 0)"
      pattern: "require_once.*includes"
    - from: "bootstrap/register-mcp-server.php"
      to: "MCP Adapter v0.5.0 at wp-content/mu-plugins/vendor/wordpress/mcp-adapter/"
      via: "mcp_adapter_init action → $adapter->create_server() with server_route_namespace/server_route/tools positional names"
      pattern: "add_action\\(\\s*['\"]mcp_adapter_init['\"]"
    - from: "includes/cache.php"
      to: "WooCommerce order status transitions"
      via: "woocommerce_order_status_changed action → bulk DELETE transients with kmn_ prefix"
      pattern: "woocommerce_order_status_changed"

user_setup: []
---

<objective>
Fill the empty plugin scaffold from Phase 15 with a working bootstrap: main plugin file loads composer autoload, registers WP 6.9+ and WC dependency guards, and lazy-loads includes. The MCP server at `/wp-json/mcp/kmn-revenue` goes live with an empty tools array (no abilities registered yet — those land in Plan 16-02). Shared helpers (sql-helpers, cache, response, rate-limit stub) are production-ready so Plan 16-02 ability files only contain ability-specific logic. WP-CLI command `wp kmn ability test` is wired for fast feedback during Plan 16-02 development.

Purpose: Deliver MCPAPP-WP-01..03 platform-layer pattern and ABIL-SCAF-01..05 scaffold. Establish `create_server()` contract exactly (per RESEARCH.md §B1) so Plan 16-02 and Plan 16-03 work without scaffold surprises. Empty-tools-list round-trip is the first integration test that catches MCP Adapter integration bugs before ability code is written.

Output: Activatable plugin on DDEV that owns `/wp-json/mcp/kmn-revenue` and returns a valid `{"result":{"tools":[]}}` for `tools/list`. All helpers load clean. WP-CLI command prints the correct "no abilities registered" message.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-kmn-revenue-abilities-wp-plugin/16-RESEARCH.md
@.planning/phases/15-local-dev-synthetic-seeder/15-PATTERNS.md
@.planning/phases/15-local-dev-synthetic-seeder/15-02-SUMMARY.md
@docs/ideas/WP_BRIDGE_ARCHITECTURE.md
@maxi-ai/abilities/woocommerce/list-orders.php
@maxi-ai/maxi-ai.php
@wordpress-plugins/kmn-revenue-abilities/kmn-revenue-abilities.php
@wordpress-plugins/kmn-revenue-abilities/composer.json

<interfaces>
<!-- Corrected MCP Adapter v0.5.0 create_server() signature per RESEARCH.md §B1 -->
<!-- Critical: WP_BRIDGE_ARCHITECTURE.md §5 uses WRONG parameter names — use these instead. -->

```php
// $adapter is \WP\MCP\Core\McpAdapter
public function create_server(
    string $server_id,                          // 'kmn-revenue'
    string $server_route_namespace,             // 'mcp'           — NOT 'rest_namespace'
    string $server_route,                       // 'kmn-revenue'   — NOT 'rest_route'
    string $server_name,                        // 'KMN Revenue Intelligence'
    string $server_description,
    string $server_version,                     // '1.0.0'
    array  $mcp_transports,                     // [ HttpTransport::class ]
    ?string $error_handler,                     // ErrorLogMcpErrorHandler::class
    ?string $observability_handler = null,      // NullMcpObservabilityHandler::class
    array  $tools = [],                         // ability IDs     — NOT 'abilities'
    array  $resources = [],
    array  $prompts = [],
    ?callable $transport_permission_callback = null
)
```

Full class names:
- `\WP\MCP\Transport\HttpTransport`
- `\WP\MCP\Infrastructure\ErrorHandling\ErrorLogMcpErrorHandler`
- `\WP\MCP\Infrastructure\Observability\NullMcpObservabilityHandler`

Must be called INSIDE `add_action('mcp_adapter_init', function ($adapter) {...})`. Calling outside raises `_doing_it_wrong`.

Endpoint resulting URL: `/wp-json/{server_route_namespace}/{server_route}` = `/wp-json/mcp/kmn-revenue`.
</interfaces>

<seeded_data_facts>
- Table prefix on DDEV: `s7uy9uh34_` — always resolve via `$wpdb->prefix`, never hardcode
- MCP Adapter v0.5.0 installed at `wp-content/mu-plugins/vendor/wordpress/mcp-adapter/` (via Phase 15 mu-plugin loader)
- Plugin's own `vendor/` directory must be created via `composer install --no-dev` inside the plugin directory (installs a SECOND copy; standard WP plugin convention)
- Service account: `dev-admin` (user ID=1), App password `6MEkttWMf26sFbGzpQ3ZfEuQ` (Phase 15)
- Maxi AI plugin coexists — isolated via distinct `server_id`
- Composer pin: `"wordpress/mcp-adapter": "0.5.0"` (exact, pre-1.0) — do NOT loosen to `^0.5.0`
</seeded_data_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install composer dependency + scaffold shared helpers + expand main plugin file</name>
  <files>
    wordpress-plugins/kmn-revenue-abilities/composer.json,
    wordpress-plugins/kmn-revenue-abilities/vendor/ (generated),
    wordpress-plugins/kmn-revenue-abilities/kmn-revenue-abilities.php,
    wordpress-plugins/kmn-revenue-abilities/includes/response.php,
    wordpress-plugins/kmn-revenue-abilities/includes/cache.php,
    wordpress-plugins/kmn-revenue-abilities/includes/sql-helpers.php,
    wordpress-plugins/kmn-revenue-abilities/includes/rate-limit.php,
    wordpress-plugins/kmn-revenue-abilities/includes/cli-command.php
  </files>

  <action>
**WHAT:** Install the plugin's composer dependency, create five `includes/` helper files, expand the main plugin file from shell-mode to working-loader mode. Mirror Maxi AI style verbatim (procedural + underscore-prefixed classes, ABSPATH guard in every PHP file).

**WHERE:**
- `wordpress-plugins/kmn-revenue-abilities/` — all files live here; this is the plugin root

**WHY:**
- ABIL-SCAF-02 requires `vendor/` committed with plugin (REQUIREMENTS.md). Research confirmed composer pin `0.5.0` exact (not `^0.5.0`) per RESEARCH §E4; the scaffold already has this. Run `composer install` inside DDEV web container so platform PHP version matches.
- ABIL-SCAF-04 requires `includes/sql-helpers.php` + `includes/cache.php` as shared helpers.
- 15-PATTERNS.md §"Response envelope" says define our own `kmn_revenue_response()` helper — fork `maxi_ai_response()` minus the `_meta.operator_notes_revision` field. Put in `includes/response.php` (NOT inside cache.php per original scaffold note — splits cleaner).
- RESEARCH §E1 recommends deferring rate limit to v3.1 but pre-creating a stub so activation later is a one-line flip.
- WP-CLI `wp kmn ability test <id>` (per the prompt's "Optional" recommendation — accept: fast feedback during Plan 16-02).

**STEPS:**

1. **Install composer deps inside DDEV** (so platform PHP matches):
   ```bash
   ddev exec 'cd /var/www/html/wp-content/plugins/kmn-revenue-abilities && composer install --no-dev --optimize-autoloader'
   ```
   Verify `vendor/wordpress/mcp-adapter/mcp-adapter.php` exists and contains `Version: 0.5.0`.
   Commit the whole `vendor/` tree (standard WP plugin convention per 15-PATTERNS.md; `.gitignore` has no rule against it).

2. **Expand `kmn-revenue-abilities.php`** from the current 70-line shell. Keep the existing plugin header, ABSPATH guard, composer autoload line, and guard-then-deactivate logic. Add:
   - `const ABILITY_NS = 'kmn/';` class constant
   - Loader method `load_includes()` that `require_once`s every `includes/*.php` via `glob( __DIR__ . '/includes/*.php' )` — mirrors `maxi-ai/maxi-ai.php:31-45`.
   - Loader method `load_bootstrap()` that `require_once`s `bootstrap/register-mcp-server.php`.
   - Loader method `load_abilities()`: lazy pattern per 15-PATTERNS.md "Hook registration pattern" (maxi-ai:77-86):
     ```php
     private static function load_abilities() {
         if ( defined( 'WP_CLI' ) && WP_CLI ) {
             self::require_ability_files();
             return;
         }
         add_action( 'rest_api_init', [ self::class, 'require_ability_files' ], 0 );
     }
     private static function require_ability_files() {
         foreach ( glob( __DIR__ . '/abilities/*.php' ) as $f ) { require_once $f; }
     }
     ```
   - Call `self::load_includes()`, `self::load_bootstrap()`, `self::load_abilities()` from `init()` AFTER the WP and WC guards pass.
   - Keep existing `register_activation_hook`, `register_deactivation_hook`, and `add_action('plugins_loaded', ['KMN_Revenue_Abilities', 'init'])`.

3. **Create `includes/response.php`** (~35 lines):
   - ABSPATH guard.
   - Function `kmn_revenue_response( bool $success, array $data = [], ?string $error = null, array $extra_meta = [] ): array` returning:
     ```php
     [
       'success' => (bool) $success,
       'data'    => $data,
       'error'   => $error,
       '_meta'   => array_merge([
         'plugin'       => 'kmn-revenue-abilities',
         'version'      => KMN_Revenue_Abilities::VERSION,
         'calculated_at' => gmdate( 'c' ),
       ], $extra_meta),
     ];
     ```
   - On failure: `error_log( sprintf( '[kmn-revenue-abilities] %s', $error ) )` before return.

4. **Create `includes/cache.php`** (~80 lines):
   - ABSPATH guard.
   - Constants: `KMN_REVENUE_TTL_RUN_RATE = 5 * MINUTE_IN_SECONDS`, `KMN_REVENUE_TTL_DEFAULT = 15 * MINUTE_IN_SECONDS`, `KMN_REVENUE_TTL_REPEAT = HOUR_IN_SECONDS`, `KMN_REVENUE_TTL_BASKET = HOUR_IN_SECONDS`, `KMN_REVENUE_TTL_BRIEFING = 5 * MINUTE_IN_SECONDS`.
   - `kmn_revenue_cache_key( string $ability_id, array $input ): string`:
     - Normalize: `ksort($input)`, coerce arrays with `array_values`, then `sha1($ability_id . '|' . wp_json_encode($input))`.
     - Return `'kmn_' . substr(sha1(...), 0, 40)` (keeps <172 char WP transient-key limit).
     - Record a human-readable hint in a doc comment — cache keys are opaque.
   - `kmn_revenue_cached( string $key, int $ttl, callable $producer ): mixed`:
     - Read-through. If `get_transient($key) !== false` return it (with explicit check vs `false`, since WP_Error from producer shouldn't be treated as cache hit).
     - Else call `$producer()`. If result is WP_Error, do NOT cache. Else `set_transient($key, $result, $ttl)`.
     - Return the result.
     - Honor `$input['_skip_cache'] === true` by bypassing the cache; dev-only hook per RESEARCH §I2 mitigation. Implement as optional second-arg override: `kmn_revenue_cached($key, $ttl, $producer, $skip = false)`.
   - `kmn_revenue_invalidate_all(): int`:
     - `global $wpdb;`
     - `$wpdb->query("DELETE FROM {$wpdb->options} WHERE option_name LIKE '\\_transient\\_kmn\\_%' OR option_name LIKE '\\_transient\\_timeout\\_kmn\\_%'")`
     - Return affected row count (int).
   - Hook: `add_action('woocommerce_order_status_changed', 'kmn_revenue_invalidate_all', 10, 0);` — registered at file load.

5. **Create `includes/sql-helpers.php`** (~150 lines):
   - ABSPATH guard.
   - `kmn_revenue_get_utc_offset( ?string $timezone = null, ?DateTimeImmutable $at = null ): string`:
     - If `$timezone` is null, use `wp_timezone()`.
     - Accept both IANA name (`Europe/Vienna`) and numeric offset (`+02:00`). Detect via regex `^[+-]\d{2}:\d{2}$`.
     - If numeric: return as-is (normalized to `+HH:MM`).
     - If IANA: `(new DateTimeImmutable('now', new DateTimeZone($timezone)))->format('P')` → `+02:00`.
     - At the moment `$at` (defaults to now) — DST-aware; accepts ≤1h error on transition days per RESEARCH §C6.
     - On invalid input (unknown IANA): return `'+00:00'` and `error_log` a warning.
   - `kmn_revenue_resolve_tz_offset( array $input, ?DateTimeImmutable $at = null ): string` — convenience wrapper that reads `$input['timezone'] ?? null`.
   - `kmn_revenue_status_whitelist( array $input, array $default = ['wc-completed','wc-processing'] ): array`:
     - Read `$input['status'] ?? $default`.
     - Filter each to `/^wc-[a-z-]+$/` regex only; drop invalid entries.
     - Return unique, preserving order.
     - Caller uses this as `$statuses`; SQL then builds placeholders via `kmn_revenue_prepare_in_placeholders($statuses)`.
   - `kmn_revenue_prepare_in_placeholders( array $values ): string`:
     - Return `'%s,%s,%s'` with one `%s` per value, comma-joined.
     - Zero-length input → return `"''"` (SQL empty-string that matches nothing).
   - `kmn_revenue_utc_bounds_for_date( string $yyyy_mm_dd, string $offset ): array`:
     - Local-tz midnight of `$yyyy_mm_dd` converted to UTC bounds.
     - Returns `[string $start_utc, string $end_utc]` — pairs suitable for `WHERE date_created >= %s AND date_created < %s`.
     - Use `DateTimeImmutable::createFromFormat` with numeric-offset TZ.
   - `kmn_revenue_utc_bounds_for_window( string $ref_date, int $days, string $offset ): array`:
     - `$ref_date - $days` (inclusive) to `$ref_date` (exclusive of next day).
     - Return `[$start_utc, $end_utc]`.
   - `kmn_revenue_set_query_timeout_ms( int $ms = 2000 ): void`:
     - `$GLOBALS['wpdb']->query( $GLOBALS['wpdb']->prepare('SET SESSION MAX_EXECUTION_TIME=%d', $ms) );`
     - Wrapper so every ability calls the same thing before heavy queries (ABIL-QA-03).

6. **Create `includes/rate-limit.php`** (~25 lines, DEFERRED):
   - ABSPATH guard.
   - Docblock at top explaining "**DEFERRED to v3.1** — MCP Adapter v0.5.0 does not provide rate limit middleware (verified in RESEARCH §B6). This file exists as a stub so activation is a one-line flip in Phase 16 or later. Do NOT wire up yet."
   - Function `kmn_revenue_rate_limit_check( string $ability_id, int $limit = 60, int $window_s = 60 ): bool`:
     - Compute key `sprintf('kmn_rl_%d_%s', get_current_user_id(), $ability_id)`.
     - Read int from transient; increment; set back with remaining TTL = window_s if new, else keep existing.
     - Return `true` if under limit, `false` else.
     - **Body commented out** with TODO line saying "activate in v3.1 once we have call-volume evidence". Stub returns `true` unconditionally.

7. **Create `includes/cli-command.php`** (~60 lines):
   - ABSPATH guard.
   - `if ( ! ( defined('WP_CLI') && WP_CLI ) ) { return; }` guard.
   - Class `KMN_Revenue_CLI`:
     - Method `ability( $args, $assoc_args )` mapping to `wp kmn ability ...`:
       - `$args[0]` is subcommand: `test`, `list`.
       - `test <id>`: resolve via `wp_get_ability( 'kmn/' . $id )` (strip `kmn/` if caller re-included), invoke `$ability->get_callback('execute')` directly with parsed `--input=JSON` assoc arg (defaulting to `{}`), print result via `WP_CLI::log(print_r(..., true))`.
       - `list`: `foreach ( wp_get_abilities() as $ability ) { if ( str_starts_with( $ability->get_id(), 'kmn/' ) ) WP_CLI::log( $ability->get_id() ); }` — in Plan 16-01 this prints nothing (no abilities yet); add a sentinel `WP_CLI::log( 'no abilities registered yet' ); return;` when the loop yielded zero matches.
   - Register: `WP_CLI::add_command( 'kmn ability', [ 'KMN_Revenue_CLI', 'ability' ] );` at file bottom (still WP_CLI-guarded).

**Style conventions (15-PATTERNS.md mandatory):**
- Mirror `maxi-ai/abilities/woocommerce/list-orders.php` for formatting (2-space indent? NO — WP core is 4-space/tab. Follow Maxi's tab indent).
- Every function gets a `/**` docblock with `@param` / `@return`.
- Prefix ALL free functions with `kmn_revenue_`. No PSR-4 namespaces.
- `final class` for `KMN_Revenue_Abilities` and `KMN_Revenue_CLI` — mirrors `final class Maxi_AI`.

**AVOID (explicit):**
- Do NOT introduce PSR-4 namespaces under `KMN\\RevenueAbilities\\` — 15-PATTERNS.md §"Conventions to Follow" rules that out; procedural Maxi-style is mandatory.
- Do NOT copy Maxi's `register-abilities-with-mcp.php` pattern (loops all abilities, fires `mcp_register_ability_tool`). We use `$tools: [...]` enumeration instead — see Task 2.
- Do NOT include `.md` docs files outside `readme.md` (the `.htaccess` blocks `.md` from web access, but it's fine to add internal docs — just scope-creep risk).
- Do NOT wire up the rate limiter body. Keep it a stub per RESEARCH §E1.

**Rollback:** `git checkout wordpress-plugins/kmn-revenue-abilities/` reverts. The plugin is NEW this phase (Phase 15 left a 4-file shell); no production impact. DDEV plugin deactivation on rollback: `ddev wp plugin deactivate kmn-revenue-abilities`.
  </action>

  <verify>
    <automated>
# 1. Composer vendor installed + correct version
ddev exec 'test -f /var/www/html/wp-content/plugins/kmn-revenue-abilities/vendor/wordpress/mcp-adapter/mcp-adapter.php' && \
ddev exec "grep -q 'Version: 0.5.0' /var/www/html/wp-content/plugins/kmn-revenue-abilities/vendor/wordpress/mcp-adapter/mcp-adapter.php"

# 2. PHP lint all new files
for f in wordpress-plugins/kmn-revenue-abilities/kmn-revenue-abilities.php \
         wordpress-plugins/kmn-revenue-abilities/includes/response.php \
         wordpress-plugins/kmn-revenue-abilities/includes/cache.php \
         wordpress-plugins/kmn-revenue-abilities/includes/sql-helpers.php \
         wordpress-plugins/kmn-revenue-abilities/includes/rate-limit.php \
         wordpress-plugins/kmn-revenue-abilities/includes/cli-command.php; do
  ddev exec "php -l /var/www/html/wp-content/plugins/kmn-revenue-abilities/$(basename $(dirname $f))/$(basename $f) 2>&1 | grep -q 'No syntax errors'" || exit 1
done

# 3. Plugin activates clean
ddev wp plugin activate kmn-revenue-abilities 2>&1 | tee /tmp/act.log
grep -q "Plugin .kmn-revenue-abilities. activated" /tmp/act.log

# 4. Helpers callable without fatal
ddev wp eval 'echo function_exists("kmn_revenue_response") ? "y" : "n";' | grep -q '^y$'
ddev wp eval 'echo function_exists("kmn_revenue_cache_key") ? "y" : "n";' | grep -q '^y$'
ddev wp eval 'echo function_exists("kmn_revenue_cached") ? "y" : "n";' | grep -q '^y$'
ddev wp eval 'echo function_exists("kmn_revenue_get_utc_offset") ? "y" : "n";' | grep -q '^y$'

# 5. WP-CLI ability list works (prints sentinel because no abilities yet)
ddev wp kmn ability list 2>&1 | grep -q "no abilities registered yet"
    </automated>
  </verify>

  <done>
- Plugin's `vendor/wordpress/mcp-adapter/mcp-adapter.php` exists, version 0.5.0 confirmed.
- `ddev wp plugin activate kmn-revenue-abilities` succeeds; no PHP fatals in debug.log.
- All 5 helper functions resolvable from `wp eval`.
- `ddev wp kmn ability list` prints "no abilities registered yet" and exits 0.
- `php -l` passes on every new `.php` file under the plugin directory.
- `woocommerce_order_status_changed` hook registered (verify: `ddev wp eval 'var_export(has_action("woocommerce_order_status_changed","kmn_revenue_invalidate_all"));'` prints `int(10)` not `bool(false)`).
  </done>
</task>

<task type="auto">
  <name>Task 2: MCP server registration (bootstrap/register-mcp-server.php) + empty tools/list round-trip + readme with App Password rotation runbook</name>
  <files>
    wordpress-plugins/kmn-revenue-abilities/bootstrap/register-mcp-server.php,
    wordpress-plugins/kmn-revenue-abilities/readme.md
  </files>

  <action>
**WHAT:** Register the MCP server with the corrected `create_server()` signature (per RESEARCH §B1), enumerate 5 ability IDs in `$tools` array (they'll be created in Plan 16-02 — we pre-list them now; MCP Adapter logs a warning for missing abilities but does not fail). Extend readme with install/DDEV/ability-test/rotation instructions.

**WHERE:**
- `wordpress-plugins/kmn-revenue-abilities/bootstrap/register-mcp-server.php` — new file (~60 lines)
- `wordpress-plugins/kmn-revenue-abilities/readme.md` — replace the stub with real content (~200 lines)

**WHY:**
- ABIL-SCAF-03 requires `$adapter->create_server()` exposes `/wp-json/mcp/kmn-revenue`.
- MCPAPP-WP-02 requires composer-managed adapter + `mcp_adapter_init` hook + separate `server_id`.
- MCPAPP-WP-03 requires Application Password rotation runbook — lives in BOTH plugin readme AND `docs/DECISIONS.md`. We add to plugin readme in this task; the `docs/DECISIONS.md` entry is a docs-memory-agent task at phase close (outside this plan).
- RESEARCH §B1 corrected three parameter-name errors from WP_BRIDGE_ARCHITECTURE §5. Use the VERIFIED signature, not the doc's wrong version.
- RESEARCH §I2 "Biggest unknown #1": verify empty-tools-list round-trip BEFORE adding abilities so name-sanitization or mcp_adapter_init timing bugs surface early. That's exactly what this task's verification does.

**STEPS:**

1. **Create `bootstrap/register-mcp-server.php`:**
   ```php
   <?php
   if ( ! defined( 'ABSPATH' ) ) {
       exit;
   }

   /**
    * Registers the kmn-revenue MCP server with the adapter.
    *
    * Hook: mcp_adapter_init (fires on rest_api_init priority 15, after
    * wp_abilities_api_init priority 10 on the same rest_api_init).
    *
    * Signature corrected from WP_BRIDGE_ARCHITECTURE.md §5 per live-verified
    * McpAdapter.php:148-170. Do NOT use 'rest_namespace' / 'rest_route' /
    * 'abilities' — the real names are server_route_namespace / server_route / tools.
    *
    * @param \WP\MCP\Core\McpAdapter $adapter
    */
   add_action( 'mcp_adapter_init', function ( $adapter ) {

       // Guard against missing classes (unlikely, but survives partial installs).
       if ( ! class_exists( '\\WP\\MCP\\Transport\\HttpTransport' ) ) {
           error_log( '[kmn-revenue-abilities] HttpTransport class missing; skipping server registration.' );
           return;
       }

       $adapter->create_server(
           server_id:             'kmn-revenue',
           server_route_namespace:'mcp',
           server_route:          'kmn-revenue',
           server_name:           'KMN Revenue Intelligence',
           server_description:    'WooCommerce revenue analytics for KAMANIN Portal.',
           server_version:        '1.0.0',
           mcp_transports:        [ \WP\MCP\Transport\HttpTransport::class ],
           error_handler:         \WP\MCP\Infrastructure\ErrorHandling\ErrorLogMcpErrorHandler::class,
           observability_handler: \WP\MCP\Infrastructure\Observability\NullMcpObservabilityHandler::class,
           tools: [
               // Registered in Plan 16-02. Adapter logs a warning for each missing
               // ability but does not fail the server registration.
               'kmn/weekly-heatmap',
               'kmn/repeat-metrics',
               'kmn/revenue-run-rate',
               'kmn/market-basket',
               'kmn/weekly-briefing-data',
           ],
           resources: [],
           prompts:   []
       );
   } );
   ```

   Key details (MUST honor):
   - PHP 8 named args — plugin header already requires PHP 8.1+, scaffold confirmed.
   - Ability IDs listed eagerly. For Plan 16-01 run-through none will resolve, adapter returns empty `tools/list` (verified in RESEARCH §I2 Mitigation 1 — planned behavior, not bug).
   - Tool name sanitization is done by the adapter: `kmn/weekly-heatmap` → MCP tool name `kmn-weekly-heatmap` (RESEARCH §G1 + §E5). We register the SLASHED form here; the HYPHENATED form appears only in `tools/list` responses and `tools/call` `name` field. Document this in readme.

2. **Replace `readme.md`** with:

   ```markdown
   # kmn-revenue-abilities

   KAMANIN WordPress companion plugin exposing 5 WooCommerce revenue-analytics abilities
   over the MCP Adapter at `/wp-json/mcp/kmn-revenue`.

   ## Requirements

   - WordPress 6.9+
   - PHP 8.1+
   - WooCommerce (HPOS enabled)
   - MySQL 8.0+ (for `ROW_NUMBER()` + `SET SESSION MAX_EXECUTION_TIME`)
   - Composer (for installing `wordpress/mcp-adapter` v0.5.0)

   ## Installation (DDEV / local development)

       cd /mnt/g/01_OPUS/Projects/PORTAL/wordpress-plugins/kmn-revenue-abilities
       ddev exec 'cd /var/www/html/wp-content/plugins/kmn-revenue-abilities && composer install --no-dev --optimize-autoloader'
       ddev wp plugin activate kmn-revenue-abilities

   Verify the MCP endpoint responds:

       curl -sk -u dev-admin:$WP_APP_PASS -X POST \
         https://summerfield.ddev.site/wp-json/mcp/kmn-revenue \
         -H 'Content-Type: application/json' \
         -H 'Accept: application/json,text/event-stream' \
         -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' \
         | jq .result.tools

   ## Tool name sanitization

   The MCP Adapter sanitizes ability IDs for MCP tool names:
   slashes become hyphens. Our abilities are registered under these IDs
   and exposed as these MCP tool names:

   | Ability ID              | MCP tool name              |
   |-------------------------|----------------------------|
   | `kmn/weekly-heatmap`    | `kmn-weekly-heatmap`       |
   | `kmn/repeat-metrics`    | `kmn-repeat-metrics`       |
   | `kmn/revenue-run-rate`  | `kmn-revenue-run-rate`     |
   | `kmn/market-basket`     | `kmn-market-basket`        |
   | `kmn/weekly-briefing-data` | `kmn-weekly-briefing-data` |

   Callers (MCP clients, mcp-poc) use the hyphenated form in `tools/call`.

   ## WP-CLI ability test

   Direct (no HTTP) execution of an ability for development:

       ddev wp kmn ability list
       ddev wp kmn ability test kmn/weekly-heatmap --input='{"weeks":8}'

   ## Application Password Rotation Runbook

   The MCP server authenticates via WordPress Application Passwords.
   Each client site uses a dedicated service account. Rotation is manual.

   **Local DDEV (Summerfield clone):**
   1. WP Admin → Users → `dev-admin` → scroll to Application Passwords.
   2. Revoke the existing entry named `mcp-dev` (if present).
   3. Enter `mcp-dev` → Add New Application Password → copy the 24-char password.
   4. Update `mcp-poc/.env.local`:
      - `WOOCOMMERCE_WP_USER=dev-admin`
      - `WOOCOMMERCE_WP_APP_PASS=<new password, spaces removed>`
   5. Restart mcp-poc dev process.

   **Production (Summerfield live, MBM):**
   1. Create a dedicated `kmn-analytics-bot` user with **Shop Manager** role.
      Shop Manager has `manage_woocommerce` but not `manage_options` — least-privilege.
   2. Generate Application Password via WP Admin (same steps as above).
   3. Store the password in the client's secrets vault.
   4. Rotation cadence: every 90 days, or immediately on any suspected compromise.
   5. After rotation update the mcp-poc Vercel env vars (`WOOCOMMERCE_WP_USER`,
      `WOOCOMMERCE_WP_APP_PASS`, `KMN_BRIDGE_URL`) and redeploy.

   **Revoking a compromised password** WP Admin → Users → {account} → Application
   Passwords → Revoke. Takes effect immediately; all mcp-poc calls using the
   revoked password start returning HTTP 401.

   ## File layout

       kmn-revenue-abilities.php              # plugin header + loader
       composer.json                          # wordpress/mcp-adapter: 0.5.0
       vendor/                                # committed composer tree
       bootstrap/register-mcp-server.php      # $adapter->create_server()
       abilities/                             # (Plan 16-02) one file per ability
       includes/
         ├── response.php                     # kmn_revenue_response()
         ├── cache.php                        # kmn_revenue_cache_key() + transient hooks
         ├── sql-helpers.php                  # date/timezone/status helpers
         ├── rate-limit.php                   # STUB — deferred to v3.1
         └── cli-command.php                  # wp kmn ability test
       scripts/
         └── verify-wp-bridge.sh              # (Plan 16-03) integration test
       .htaccess                              # blocks .md/.txt/.example
   ```

   Note: the existing scaffold `.htaccess` already blocks `.md` from web access, so including `readme.md` in the plugin is safe.

**AVOID:**
- Do NOT use `rest_namespace` / `rest_route` / `abilities` as parameter names in `create_server()` — those are the WP_BRIDGE_ARCHITECTURE.md errors. The research doc is the source of truth.
- Do NOT remove the 5 ability IDs from `$tools` even though they don't exist yet — pre-listing them makes Plan 16-02 a pure additive change.
- Do NOT include Production Summerfield / MBM credentials in the readme. Use placeholder language only.

**Rollback:** `git rm bootstrap/register-mcp-server.php` + `git checkout readme.md`. `ddev wp plugin deactivate kmn-revenue-abilities`. The MCP endpoint disappears immediately.
  </action>

  <verify>
    <automated>
# 1. Lint
ddev exec 'php -l /var/www/html/wp-content/plugins/kmn-revenue-abilities/bootstrap/register-mcp-server.php' | grep -q 'No syntax errors'

# 2. Plugin reactivates after adding bootstrap file
ddev wp plugin deactivate kmn-revenue-abilities
ddev wp plugin activate kmn-revenue-abilities 2>&1 | grep -q 'activated'

# 3. MCP endpoint responds 200 with valid JSON-RPC tools/list (empty tools array expected)
resp=$(curl -sk -u "dev-admin:6MEkttWMf26sFbGzpQ3ZfEuQ" \
  -X POST https://summerfield.ddev.site/wp-json/mcp/kmn-revenue \
  -H 'Content-Type: application/json' \
  -H 'Accept: application/json,text/event-stream' \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}')
echo "$resp" | jq -e '.result.tools' > /dev/null

# 4. tools/list returns 0 tools (abilities not yet registered — pre-listed IDs log warnings but don't appear)
count=$(echo "$resp" | jq '.result.tools | length')
[[ "$count" == "0" ]] || { echo "expected 0 tools, got $count"; exit 1; }

# 5. Wrong password → HTTP 401
http=$(curl -sk -o /dev/null -w '%{http_code}' -u "dev-admin:wrong" \
  -X POST https://summerfield.ddev.site/wp-json/mcp/kmn-revenue \
  -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}')
[[ "$http" == "401" ]]

# 6. readme.md contains rotation runbook
grep -q "Application Password Rotation Runbook" wordpress-plugins/kmn-revenue-abilities/readme.md
grep -q "Tool name sanitization" wordpress-plugins/kmn-revenue-abilities/readme.md
    </automated>
  </verify>

  <done>
- `POST /wp-json/mcp/kmn-revenue` with `tools/list` returns 200 + `{result:{tools:[]}}`.
- Wrong App Password → HTTP 401 (WP Application Password validator).
- Plugin activates/deactivates cleanly on DDEV without fatal.
- `bootstrap/register-mcp-server.php` uses the CORRECTED v0.5.0 signature (server_route_namespace / server_route / tools — NOT rest_namespace / rest_route / abilities).
- `readme.md` includes: install steps, WP-CLI ability-test command, tool-name-sanitization mapping table, App Password rotation runbook (dev + prod).
- Plugin PHP error log has no uncaught errors — only the adapter's benign "ability kmn/xxx not registered" warnings (those resolve in Plan 16-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mcp-poc/Node → WordPress | HTTPS + Basic Auth (Application Password). Untrusted caller input reaches PHP via `$input` array on every `execute_callback` (none yet in this plan). |
| Plugin PHP → Database | `$wpdb` same-process; `$input` values enter SQL ONLY through `$wpdb->prepare()`. No execute_callback bodies in Plan 16-01 (they land in 16-02), but helpers set the contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Spoofing | Basic Auth credentials in transit | mitigate | HTTPS-only (mkcert-trusted on DDEV, Let's Encrypt on prod). readme documents rotation cadence and revocation steps. |
| T-16-01-02 | Tampering | Future ability SQL injection via `$input` | mitigate | `includes/sql-helpers.php::kmn_revenue_status_whitelist()` enforces regex on status strings; `kmn_revenue_prepare_in_placeholders()` builds `%s` placeholders only; abilities must use `$wpdb->prepare()` (contract enforced by `scripts/audit-sql.sh` in Plan 16-03). |
| T-16-01-03 | Repudiation | No per-call audit log for ability invocation | accept | POC scope. `ErrorLogMcpErrorHandler` captures errors to PHP error log. Postponed to v3.1 if audit trail needed. |
| T-16-01-04 | Information Disclosure | Credential leak via PHP error log | mitigate | `kmn_revenue_response()` error path does NOT log `$input` or `$_SERVER['HTTP_AUTHORIZATION']`. Adapter's default error handler is safe per RESEARCH §Security Domain. |
| T-16-01-05 | Denial of Service | Unrate-limited call flood on MCP endpoint | accept | Deferred to v3.1 per RESEARCH §E1. `SET SESSION MAX_EXECUTION_TIME=2000` (ABIL-QA-03) caps per-query time; transient cache absorbs repeat load. Stub `includes/rate-limit.php` exists for future activation. |
| T-16-01-06 | Elevation | Non-admin user calling via forged session | mitigate | WP core resolves `current_user` via `determine_current_user` filter chain BEFORE MCP Adapter dispatch (RESEARCH §A2). Ability-level `permission_callback` (Plan 16-02) gates on `manage_woocommerce`. In this plan empty tools mean no ability bodies to protect. |
| T-16-01-07 | Tampering | Composer dep swap (malicious `wordpress/mcp-adapter` version) | mitigate | Exact version pin `0.5.0` (not `^0.5.0`) per research. `vendor/` committed — no install-time fetch in production. |
</threat_model>

<verification>
Plan-level checks (run after both tasks complete):

1. **Plugin bootstrap integration:**
   `ddev wp plugin activate kmn-revenue-abilities` succeeds on Summerfield DDEV without fatal.

2. **Endpoint smoke test** — the fundamental MCPAPP-WP-02 gate:
   ```bash
   curl -sk -u "dev-admin:$WP_APP_PASS" -X POST \
     https://summerfield.ddev.site/wp-json/mcp/kmn-revenue \
     -H 'Content-Type: application/json' \
     -H 'Accept: application/json,text/event-stream' \
     -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | jq '.result.tools | length'
   ```
   MUST print `0`.

3. **Helpers available for Plan 16-02:** every function in `includes/*.php` resolvable from `wp eval`.

4. **Maxi coexistence sanity check** (early version of ABIL-QA-05):
   ```bash
   ddev wp plugin list --status=active | grep -E 'kmn-revenue-abilities|maxi-ai'
   ```
   Both listed as active. Maxi's tools/list endpoint (different route) still returns 122 abilities.

5. **SQL helper unit probe:**
   ```bash
   ddev wp eval '
     $off = kmn_revenue_get_utc_offset("Europe/Vienna");
     echo $off;  // expect +01:00 or +02:00
   '
   ```
   MUST match `/^[+-]\d{2}:\d{2}$/`.
</verification>

<success_criteria>
Plan 16-01 is successful when:
- All `must_haves.truths` are observably true on Summerfield DDEV.
- All `must_haves.artifacts` exist with correct line-count floor and contents.
- All `must_haves.key_links` probe positively (grep / wp eval).
- `ABIL-SCAF-01..05`, `MCPAPP-WP-01..03` requirements are wholly or partially satisfied (the docs-layer portion of MCPAPP-WP-01/03 is a docs-memory-agent task).
- `tools/list` returns `{tools:[]}` — this is the first end-to-end integration test that proves adapter wiring works; catches the slash-sanitization class-not-found Class not found, and timing bugs flagged in RESEARCH §I2 before Plan 16-02 adds real abilities.
</success_criteria>

<output>
After completion, create `.planning/phases/16-kmn-revenue-abilities-wp-plugin/16-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Required sections:
- Files created (with line counts)
- Git commit(s) — single `feat(16-01): plugin bootstrap + MCP server + shared helpers`
- MCP endpoint verification: paste raw curl output of `tools/list`
- `SET SESSION MAX_EXECUTION_TIME` hook pattern established
- Handoff to Plan 16-02: ability files plug into `abilities/` directory, get auto-loaded on `rest_api_init` priority 0, appear in `tools/list` with sanitized names.
</output>
