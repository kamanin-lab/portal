---
phase: quick-260330-mp6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/modules/tickets/components/RecommendationCard.tsx
  - src/modules/tickets/components/RecommendationsBlock.tsx
  - src/modules/tickets/components/RecommendationActions.tsx  # DELETE
  - src/modules/tickets/components/TaskDetail.tsx
  - src/modules/tickets/pages/TicketsPage.tsx
  - src/shared/types/common.ts
  - src/shared/components/konto/NotificationSection.tsx
  - supabase/functions/_shared/emailCopy.ts
  - supabase/functions/clickup-webhook/index.ts
autonomous: true
requirements: [REC-POLISH]

must_haves:
  truths:
    - "RecommendationCard looks like a standard TaskCard with priority bar, status badge, due date, credits, created_by_name, list_name"
    - "Clicking a recommendation card opens TaskDetailSheet"
    - "Accept/Decline actions appear inside TaskDetail when task has recommendation tag"
    - "Accept shows date picker dialog, Decline shows optional comment dialog"
    - "new_recommendation toggle exists in notification settings"
    - "Webhook sends bell + email when recommendation tag is added to a task"
  artifacts:
    - path: "src/modules/tickets/components/RecommendationCard.tsx"
      provides: "Standard card layout matching TaskCard"
    - path: "src/modules/tickets/components/TaskDetail.tsx"
      provides: "RecommendationApproval section for tagged tasks"
    - path: "supabase/functions/clickup-webhook/index.ts"
      provides: "Tag-based recommendation notification handler"
  key_links:
    - from: "RecommendationCard"
      to: "TaskDetailSheet"
      via: "onTaskClick prop → URL param ?taskId="
    - from: "TaskDetail"
      to: "useTaskActions"
      via: "acceptRecommendation / declineRecommendation"
    - from: "clickup-webhook"
      to: "notifications + send-mailjet-email"
      via: "tag field detection → new_recommendation email type"
---

<objective>
Polish recommendations feature: make RecommendationCard match standard TaskCard layout, move accept/decline actions into TaskDetail sheet, add new_recommendation notification preference + email type, and add webhook handler for tag-based recommendation detection.

Purpose: Unify card UX, move recommendation actions to the detail sheet (consistent with credit approval pattern), enable email notifications when team tags a task as recommendation.
Output: Rewritten card, updated detail sheet, notification toggle, email copy, webhook handler.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/modules/tickets/components/TaskCard.tsx
@src/modules/tickets/components/TaskDetail.tsx
@src/modules/tickets/components/CreditApproval.tsx
@src/modules/tickets/components/RecommendationCard.tsx
@src/modules/tickets/components/RecommendationsBlock.tsx
@src/modules/tickets/components/RecommendationActions.tsx
@src/modules/tickets/pages/TicketsPage.tsx
@src/modules/tickets/hooks/useTaskActions.ts
@src/modules/tickets/lib/dictionary.ts
@src/shared/types/common.ts
@src/shared/components/konto/NotificationSection.tsx
@supabase/functions/_shared/emailCopy.ts
@supabase/functions/clickup-webhook/index.ts

<interfaces>
From src/modules/tickets/types/tasks.ts — ClickUpTask interface (has: clickup_id, name, description, status, priority, credits, due_date, created_by_name, list_name, raw_data)
From src/modules/tickets/hooks/useTaskActions.ts:
  - acceptRecommendation(taskId: string, dueDate?: number)
  - declineRecommendation(taskId: string, comment?: string)
From src/modules/tickets/lib/status-mapping.ts — mapStatus(status: string): string
From src/modules/tickets/components/CreditApproval.tsx — CreditApproval({ taskId, credits, taskName }) — pattern to follow for RecommendationApproval
From src/shared/types/common.ts:
  - NotificationPreferences interface
  - DEFAULT_NOTIFICATION_PREFERENCES constant
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite RecommendationCard + simplify RecommendationsBlock + delete RecommendationActions</name>
  <files>
    src/modules/tickets/components/RecommendationCard.tsx
    src/modules/tickets/components/RecommendationsBlock.tsx
    src/modules/tickets/components/RecommendationActions.tsx
    src/modules/tickets/pages/TicketsPage.tsx
  </files>
  <action>
**RecommendationCard.tsx — complete rewrite** to match TaskCard layout:
- Remove `onAccept` and `onDecline` props entirely. Props become: `{ task: ClickUpTask; unreadCount?: number; onTaskClick: (id: string) => void }`
- Copy TaskCard's structure exactly: outer `motion.div` with hover, inner `button` with click handler, status left border using `STATUS_BORDER_COLORS[portalStatus]`
- Add a subtle recommendation indicator: after the task name `<h3>`, add a small `HugeiconsIcon` (Idea01Icon, size 13, `text-[var(--phase-3)]`) inline — NOT the dominant amber color scheme
- Include all TaskCard meta elements: `StatusBadge`, `PriorityIcon`, `CreditBadge`, due date with calendar icon, `created_by_name`, `list_name` (if set)
- Import `mapStatus` from status-mapping, `StatusBadge`, `PriorityIcon`, `CreditBadge`, `Calendar03Icon`, `UserIcon` — same as TaskCard
- Keep `motion/react` import for hover animation, same spring config as TaskCard
- Card click calls `onTaskClick(task.clickup_id)` — opens TaskDetailSheet via URL param

**RecommendationsBlock.tsx — simplify:**
- Remove `useRef` for openAcceptRef/openDeclineRef
- Remove `RecommendationActions` import and its JSX
- Remove `onAccept`/`onDecline` pass-through to RecommendationCard
- Only pass `task` and `onTaskClick` to each RecommendationCard
- Keep the divider, section header (Idea01Icon, count badge), subtitle, grid layout, and motion card variants

**DELETE `RecommendationActions.tsx`** — the file. It is only imported in RecommendationsBlock (confirmed). Delete the file entirely.

**TicketsPage.tsx** — no changes needed. Already only passes `recommendations` and `onTaskClick` to `RecommendationsBlock`. The `onAccept`/`onDecline` were already removed from TicketsPage in the previous implementation (RecommendationsBlock managed them internally). Verify and confirm no stale imports remain.
  </action>
  <verify>
    <automated>cd G:/01_OPUS/Projects/PORTAL && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - RecommendationCard renders like TaskCard (priority bar, status badge, due date, credits, created_by, list_name, subtle lightbulb icon)
    - No accept/decline buttons on the card
    - RecommendationActions.tsx deleted
    - RecommendationsBlock simplified (no ref/modal wiring)
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Add RecommendationApproval section to TaskDetail + notification preference + email copy</name>
  <files>
    src/modules/tickets/components/TaskDetail.tsx
    src/shared/types/common.ts
    src/shared/components/konto/NotificationSection.tsx
    supabase/functions/_shared/emailCopy.ts
  </files>
  <action>
**TaskDetail.tsx — add RecommendationApproval section:**
- Create a new component `RecommendationApproval` (can be in same file if under 150 lines total, otherwise extract to `src/modules/tickets/components/RecommendationApproval.tsx`)
- Detection: `const isRecommendation = task.raw_data?.tags?.some((t: { name: string }) => t.name === 'recommendation')`
- Place it between the Description block and the Actions block (same position as CreditApproval)
- Only show when `isRecommendation` is true (regardless of status — recommendations in any non-terminal status should show accept/decline)
- Follow CreditApproval pattern (motion.div with amber border, AnimatePresence for mode switching):
  - Header: Idea01Icon + "Empfehlung" title
  - Show credits if `task.credits != null && task.credits > 0` (same format as CreditApproval)
  - Body text: "Unser Team hat eine Empfehlung für Sie erstellt."
  - Mode 'buttons': "Annehmen" button (accent/committed color) + "Ablehnen" button (outline)
  - Mode 'accepting': date picker input (same `<input type="date">` as old RecommendationActions, default 14 days from now) + "Annehmen" confirm + "Abbrechen" ghost
  - Mode 'declining': Textarea for optional comment + "Ablehnen" confirm + "Abbrechen" ghost
  - On accept: call `acceptRecommendation(taskId, dueDateMs)` from `useTaskActions`
  - On decline: call `declineRecommendation(taskId, comment)` from `useTaskActions`
- Do NOT show normal TaskActions when `isRecommendation` is true — replace the `<TaskActions>` section with `<RecommendationApproval>` for recommendation tasks
- Keep CreditApproval logic intact (it shows for `awaiting_approval` status, not for recommendations)

**src/shared/types/common.ts:**
- Add `new_recommendation: boolean` to `NotificationPreferences` interface
- Add `new_recommendation: true` to `DEFAULT_NOTIFICATION_PREFERENCES`

**src/shared/components/konto/NotificationSection.tsx:**
- Add 6th entry to `NOTIFICATION_OPTIONS` array:
  ```
  {
    key: 'new_recommendation',
    label: 'Neue Empfehlung',
    description: 'Benachrichtigung, wenn das Team eine neue Empfehlung für Sie erstellt hat.',
  }
  ```

**supabase/functions/_shared/emailCopy.ts:**
- Add `"new_recommendation"` to the `EmailType` union type
- Add `new_recommendation` entry to `EMAIL_COPY` object:
  - de:
    - subject: `(taskName: string) => "Neue Empfehlung: " + taskName`
    - title: "Neue Empfehlung"
    - greeting: greetDe
    - body: `(taskName: string) => "Unser Team hat eine neue Empfehlung für Sie erstellt: \"<strong>" + taskName + "</strong>\". Bitte sehen Sie sich den Vorschlag an und entscheiden Sie, ob Sie ihn annehmen möchten."`
    - cta: "Empfehlung ansehen"
  - en:
    - subject: `(taskName: string) => "New recommendation: " + taskName`
    - title: "New Recommendation"
    - greeting: greetEn
    - body: `(taskName: string) => "Your team has created a new recommendation for you: \"<strong>" + taskName + "</strong>\". Please review the suggestion and decide if you'd like to accept it."`
    - cta: "View recommendation"
  </action>
  <verify>
    <automated>cd G:/01_OPUS/Projects/PORTAL && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - TaskDetail shows RecommendationApproval block when task has recommendation tag
    - Accept flow: date picker → acceptRecommendation call
    - Decline flow: optional comment → declineRecommendation call
    - Normal TaskActions hidden for recommendation tasks
    - NotificationPreferences has new_recommendation field
    - NotificationSection shows 6th toggle
    - emailCopy.ts has new_recommendation email type
    - TypeScript compiles clean
  </done>
</task>

<task type="auto">
  <name>Task 3: Add recommendation tag detection to clickup-webhook</name>
  <files>
    supabase/functions/clickup-webhook/index.ts
  </files>
  <action>
**Add tag-based recommendation notification to the webhook handler.**

In the `taskUpdated` handler section (around line 1327), BEFORE the existing `custom_field` check, add a new block that handles tag changes:

```
if (historyItem.field === "tag" && taskId && isValidTaskId(taskId)) {
  // Check if 'recommendation' tag was added
  const afterTags = historyItem.after as unknown;
  // ClickUp tag history: after is the tag name string that was added
  // or after.name contains the tag name
  const tagName = typeof afterTags === 'string' ? afterTags : (afterTags as any)?.name;

  if (tagName === 'recommendation') {
    // ... notification logic
  }
}
```

Inside the `tagName === 'recommendation'` block:
1. Log: `log.info("Recommendation tag added to task", { taskId })`
2. Fetch task visibility via `fetchTaskForVisibilityCheck(taskId, clickupApiToken, log)` — get API token from env
3. If not visible, skip (same pattern as other handlers)
4. Get task name from `taskInfo.name`
5. Find profile IDs via `findProfilesForTask(supabase, taskId, taskInfo.listId, log)`
6. Create bell notifications:
   ```
   {
     profile_id: profileId,
     type: "new_recommendation",
     title: "Neue Empfehlung",
     message: `Ihr Team hat eine Empfehlung erstellt: "${taskName}"`,
     task_id: taskId,
     is_read: false,
   }
   ```
7. Insert notifications via `supabase.from("notifications").insert(notifications)`
8. Send emails: loop profiles, check `shouldSendEmail(profile, "new_recommendation")`, call `sendMailjetEmail("new_recommendation", recipient, { firstName, taskName, taskId }, log)`
9. Return 200 response

**Also update `EMAIL_TYPE_TO_PREF_KEY` map** (around line 148):
- Add: `new_recommendation: "new_recommendation"`

**Important:** The tag detection block must come BEFORE the `custom_field` check and its fallthrough "ignored" return at line 1431. Structure:
```
if (payload.event === "taskUpdated" && historyItem) {
  // NEW: tag field → recommendation detection
  if (historyItem.field === "tag" && ...) { ... return }

  // EXISTING: custom_field → credits
  if (historyItem.field === "custom_field" && ...) { ... return }

  // EXISTING: fallthrough ignore
  log.debug("taskUpdated event ignored (not credits field)", { taskId });
  return ...
}
```

Update the fallthrough log message to: `"taskUpdated event ignored (not tag or credits field)"`
  </action>
  <verify>
    <automated>cd G:/01_OPUS/Projects/PORTAL && npx tsc --noEmit 2>&1 | head -5; echo "---"; grep -n "new_recommendation" supabase/functions/clickup-webhook/index.ts | head -10</automated>
  </verify>
  <done>
    - Webhook detects recommendation tag addition on taskUpdated events
    - Bell notification created with type new_recommendation
    - Email sent via sendMailjetEmail with new_recommendation type
    - EMAIL_TYPE_TO_PREF_KEY includes new_recommendation mapping
    - shouldSendEmail respects user's new_recommendation preference
    - Non-recommendation tag changes fall through to existing logic
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — no type errors
2. `npm run build` — production build succeeds
3. Visual check: RecommendationCard in Needs Attention tab looks like a standard TaskCard with subtle lightbulb icon
4. Click recommendation card → TaskDetailSheet opens with RecommendationApproval section
5. Accept flow: date picker → confirm → toast success
6. Decline flow: comment → confirm → toast success
7. Konto page: 6 notification toggles including "Neue Empfehlung"
8. Webhook: tag field detection code present in clickup-webhook
</verification>

<success_criteria>
- Recommendation cards visually match standard TaskCards (priority bar, status, metadata row)
- Accept/Decline actions moved to TaskDetail sheet (not on card)
- RecommendationActions.tsx deleted
- new_recommendation notification preference wired end-to-end (type → preference → toggle → email copy → webhook)
- No TypeScript errors, production build succeeds
</success_criteria>

<output>
After completion, create `.planning/quick/260330-mp6-recommendations-polish-standard-card-she/260330-mp6-SUMMARY.md`
</output>
